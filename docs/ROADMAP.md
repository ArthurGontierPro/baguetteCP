# Roadmap

Task IDs here are what sessions claim in `WORKLOG.md`. Keep IDs stable once published —
they are referenced from commits and claims.

Status: `TODO` / `WIP` / `DONE` / `BLOCKED`, plus `SUPERSEDED` / `ABSORBED` for a row whose
work moved to another ID — the row **stays**, because IDs are referenced from commits and
claims, and it names where the work went. Update your own task's status; do not reflow this
file (see the append-only note in `CLAUDE.md`).

---

## M0 — it builds

Goal: `make check` passes on a skeleton. Nothing solves anything yet.

| ID | Task | Status | Notes |
|---|---|---|---|
| M0-T1 | Project skeleton, dune files, Makefile | DONE | scaffolded |
| M0-T2 | `scripts/bootstrap.sh` installs opam + OCaml 5 + deps; `dune build` succeeds | DONE | opam 2.5.2, switch `baguette` on OCaml 5.1.1 |
| M0-T3 | The scaffolded OCaml compiles and `dune runtest` is green | DONE | 253 unit checks |
| M0-T4 | CI-equivalent: `make check` gate documented and working | DONE | green; unsolved models are `xfail` via `test/models/PENDING` |

## M1 — linear integer core, proved

Goal: solve models with only linear integer constraints, and VeriPB accepts every proof.

| ID | Task | Status | Notes |
|---|---|---|---|
| M1-T1 | `Domain` with bounds + lazy holes, unit tested | DONE | |
| M1-T2 | `Store`: domains + trail, decision levels and undo | DONE | |
| M1-T3 | `Explanation` type + arena + memoised `Deferred` | DONE | central; claim it alone |
| M1-T4 | Order encoding in `lib/proof/lit.ml`, `.opb` writer | DONE | see PROOF-FORMAT §3 |
| M1-T5 | `writer.ml`: rule emission, constraint ids, audit mode | DONE | |
| M1-T6 | FlatZinc lexer + parser for the §2.1 subset | DONE | independent of core — good parallel task |
| M1-T7 | `int_lin_le` propagator + justification | DONE | verified end to end against veripb; `rup` not `pol`, see D-0009 |
| M1-T7a | `int_lin_le` propagator and its explanations | DONE | the reference propagator; copy its shape, and its `order_reason` chains (D-0010) |
| M1-T7b | `Justify`: explanation to VeriPB rules | DONE | `Linear` states its own terms as `rup`; memo dropped in lockstep with the writer's wipe |
| M1-T7c | Order-encoding expansion of `sum a_i x_i` into a PB row | DONE | `Encoding.expand_int_lin_le`; the consistency clauses are load-bearing |
| M1-T8 | `int_lin_eq`, `int_le`, `int_lt`, `int_eq` | DONE | one instance per model row, not one fused propagator (D-0011) |
| M1-T9 | `int_lin_ne`, `int_ne` | DONE | D-0019: it does **not** need the direct encoding — a `rup` target is a clause. Propagator + proof verify, and M1-T11 wired both builtins into `compile.ml` |
| M1-T10 | DFS search with first-fail, decisions logged in the proof | DONE | the answer is right everywhere; the branch-nogood *proof* is open (D-0012) |
| M1-T11 | End-to-end: 5 small models solve and verify | DONE | **14 of 14** solve and verify, and `test/models/PENDING` is empty for the first time. `int_ne`/`int_lin_ne` wired into `compile.ml`, plus five disequality models — `ne_sat` could not see `int_ne` break, since `int_lt` alone solves it |
| M1-T12 | Make the D-0013 derivation real: `Combine`/`Weaken`/`Model_row` + the root conflict | DONE | D-0015; root refutations now verify |
| M1-T13 | The branching half of D-0013: justify a conflict reached under decisions | DONE | D-0018 + D-0021. One `rup` line per pruning, root prunings included; the nogood is ordinary `rup` along it. `chain_sat` and the three D-0012 parity models verify |
| M1-T14 | Wire the CLI: `.fzn` in, solution + proof out | DONE | 5 models solve with verified proofs; `Compile` + `Output` + `Model.check_assignment` |
| M1-T15 | Proof-mutation harness: corrupt a step, assert veripb rejects | DONE | D-0020: found `lin_unsat`'s refutation carries a unit of slack, registered known-slack with XPASS protection |
| M1-T16 | Populate the tests: a per-propagator × per-situation matrix, and a randomised differential tester | DONE | found 3 real bugs, 2 of them new. 142 + 10 checks, 17 verified break-it mutations |
| M1-T17 | Fix the three `int_ne` composition bugs M1-T16 pinned | DONE | D-0022, I-X7, I-P5. Gate green: 147 matrix + 11 fuzzer checks |
| M1-T18 | Adopt the Rust VeriPB **3.0.2** checker while still emitting format 2.0 | DONE | phase 1 of the upgrade. No proof changes: 3.0.2 reads our 2.0 proofs unchanged. Already installed at `~/.cargo/bin/veripb`, shadowed on PATH by the Python 2.2.2 |
| M1-T19 | Migrate proof **emission** to format 3.0 | DONE | D-0023/D-0024/D-0025. 3.0 was made the default here, beside a 2.0 that was still emitted and still green; it became the *only* format in M2-T16 (D-0046). Labels delivered the id trap's closure, with two qualifications recorded in D-0023 |
| M1-T20 | Correct `test_matrix.ml`'s empty-cell note and fill the four `int_ne` shape cells | DONE | M1-T17 fixed the factless trace line; the note still says those cells are unreachable |
| M1-T21 | A bool *parameter* folded to a `Const` prints as `0`/`1`, not `false`/`true` | DONE | `output.ml` sees an int and cannot tell; the type is lost in `Model.t`. Real output-correctness bug, independent of the proof |
| M1-T22 | `del range` is half-open: fix `Writer.wipe_level`, correct PROOF-FORMAT §5 | DONE | **Verified bug.** veripb 3.0.2 deletes `[LO, HI)`; we emit `del range lo hi` for the *inclusive* run, so every multi-id run leaves its last id live in the checker while `tags`/`live` drop it (I-X3). 7 lines across 5 of 14 models today. Not unsound — proof growth. See `docs/GCS-COMPARISON.md` §2.1 — **Premise re-measured before fixing**, with controls: 3.0.2 deletes `[LO, HI)`. `del range` lines 7 -> 4, the deleted set is now exact (7 ids that leaked are retired), proofs +0.4% bytes, no solution changed. The regression test asks the *checker* whether each id is still live, because the pre-existing string assertion pinned the buggy text and was green for the bug's whole life |
| M1-T23 | Checked arithmetic in `Linear`/`Ne`; cap declared domains at compile time | DONE | **The only soundness gap found.** No overflow handling exists anywhere in `lib/`; a wrapped product prunes wrongly *and* writes the `.opb` row from the same wrapped arithmetic, so veripb accepts it. Add an overflow axis to `test_matrix.ml`. Independent — good parallel task — **D-0029**, and a normative arithmetic-limit paragraph in SPEC 2.1. Gap reproduced by the orchestrator independently: the model answered UNSAT with `s VERIFIED UNSATISFIABLE` because the `.opb` row and the propagator wrapped identically. Now refused at compile time, exit 3. `lib/core/checked.ml`, cap `max_int/16` derived from a `9M+6` envelope and asserted as a test. 1005 unit checks, 17/17 models, gate peak RSS 42 MB |
| M1-T24 | Trail-cursor walk in `Engine.propagate`, replacing `Store.trail_entries` | DONE | `engine.ml:60` allocates the whole trail per propagator call and keeps only the newest `n_new`; `Store.trail_entry` already gives O(1) indexed access and its own comment says so. Pure perf, no invariant touched |
| M1-T25 | Lazy order-encoding atoms: `.opb` carries bounds, ladder steps enter the proof by `red` on demand | DONE | `declare_int` writes `hi - lo - 1` rows per variable eagerly and uncapped — `var 0..1000000: x` is a million-row `.opb`. `Encoding.ensure_direct` is already this pattern for the direct encoding. SPEC/PROOF-FORMAT §3 change + decision record. Does **not** fix width-proportional *reasons* — that is M1-T27 — **D-0031 — the row is ANSWERED, not implemented, and no code changed.** The premise does not survive the checker: nothing cites a rung (`consistency_id` has no caller in `lib/` or `bin/`), but the ladder is load-bearing anyway, because the demand is the checker's **own unit propagation**, invisible at the emission site. Strip the ladder and 7 of 20 proofs are rejected at the first trace line — orchestrator reproduced on `chain_sat` (RUP failure) and `lin_sat` (still verifies). The `red` recipe exists (rotation, not the obvious swap) and is **Θ(w²)**: 5× larger on `width_sat_depth`. Ceiling where the ladder is unused: −60% `.opb`, a constant factor, not asymptotic |
| M1-T26 | Move mutation knobs from proof text into the emitter; add a mandatory control lane | DONE | `mutate_proof.sh`'s own header records that `drop-line` now fails as a *parse* error under 3.0 (it un-defines the label), so the lane is green for a reason unrelated to the derivation — the D-0020 failure mode. GCS types the knobs next to each derivation — **D-0030**. Audited every lane by reading the checker's *message*, not its exit code: `drop-line` fails on the grammar in **both** formats, not just 3.0 as the docs said. Typed knobs now live on the `Pol.t` AST in the emitter, structurally unable to fire in a normal run (immutable field, no env var, banner on every corrupted proof, a test greps the tree). Controls are mandatory and enforced in the **type system**. Lanes 14 -> 24, those judging a derivation 8 -> 18, checks 18 -> 37, nothing dropped |
| M1-T27 | Decision record: bound-fact chains are width-proportional; state the policy | DONE | `order_reason.ml:38-43` builds one literal per value between declared and current bound (D-0010 requires the chain); D-0005's hole-span cap does not cover it. Doc-only now; the interval restatement is M4 work — **D-0028.** Row text was wrong and is corrected here: `order_reason.ml:38-43` (`lower_bound_terms`) has **no caller in `lib/`** — orchestrator confirmed, only `test_prop.ml` calls it. The live object is `Order_reason.weaken_declared` (`linear.ml:267`), which spans the **whole declared width**, not the declared-to-current prefix. Measured: two vars at `0..999999` and one row infeasible on declared bounds, so **zero prunings**, still emit a 29.8 MB proof line of ~2M literals and a 156 MB `.opb`. Reasons are already width-independent, so M2-T8 does **not** fix this |
| M1-T28 | Index-walk the trail in `linear.ml`'s `find_lo_reason`/`find_hi_reason` | DONE | Found by M1-T24. The same defect it fixed in `engine.ml`, still present twice in `lib/core/prop/linear.ml` (~172, ~186): the scan stops early on a match but `Store.trail_entries` allocates the whole trail first, and it runs **once per term per pruning** rather than once per propagator call. A downward `Store.trail_entry` walk fixes both identically. **Check M2-T8 first** — it proposes deleting this scan outright, so this may be a throwaway interim fix; take it only if M2-T8 is still far off. Measured context: M1-T24's synthetic 12000-entry-trail benchmark went 4.22s→1.56s and the agent judged the residual to be mostly these two scans — unverified — Index-walk in `find_lo_reason`/`find_hi_reason`. **Direction is load-bearing and was shown, not argued**: flipped upward, the suite goes 1051 ok/0 FAIL → 1044 ok/**10 FAIL**, every failure a veripb rejection — it returns the *oldest* entry that moved the bound, so a pruning cites a superseded reason. 60/60 model artefacts byte-identical. Measured **9.9× at N=300** on a deep chain (21.66 s → 2.18 s), and an honest null on the committed models |
| M1-T29 | The `del id` fallback line grows with the reasons retired at one level | **DONE** | Closed 2026-09-17. The run `[lo, hi]` with `hi` the newest id is emitted as a **pair** — `del range @c<lo> @c<hi>` for the half-open span, then `del id @c<hi>` — two lines of bounded length in place of one carrying `hi-lo+1` citations, so the `O(reasons)` term is gone. **Both routes the row named were rejected, on measurement.** The numeric exclusive bound works (3.0.2 tolerates one past the last id) but has no margin: two past is a hard error and the last id itself silently **under**-deletes, and it reintroduces the bare-integer citation D-0023 removed at exactly the site most likely to drift. Minting an extra always-live constraint trades a term that grows with one level's reasons for one that grows with the whole search. **The pair also makes the dangerous direction loud**, which is the real reason to prefer it: an upper bound one too high is caught either as a parse error naming the label (unassigned — D-0023's trap staying closed) or as "Trying to access constraint with ID n that has already been deleted" from the trailing `del id`. Verified independently by the orchestrator. **I-S4 is unaffected**: both rules retire ids from the same doomed set of the same `wipe_level`, so no line outlives its citee differently than before. Cost stated in the code rather than hidden — for runs of 2–4 ids the pair is a few bytes longer, and it is used unconditionally anyway, because a threshold would buy ~0.5% of proof bytes at the price of a second shape no shipped model exercises. 14 of 34 `.pbp` moved; no `.opb` and no `.out` | Found by M1-T22. When a wiped run ends at the newest id there is no label one past it, so the run is emitted as a full `del id` list rather than a range. Invisible today (+0.4% bytes, 3 of 7 sites) but O(reasons) per backtrack on a deep search. A numeric exclusive bound would keep it O(1) — 3.0.2 tolerates one past the last id, measured — but that reintroduces bare-integer citation, which D-0023's labels removed. **Unmeasured**; same gap D-0024 already flags. Revisit before M2-T3 |
| M1-T30 | A wide-domain model in `test/models/`, so the gate can see width at all | DONE | Found by M1-T27/D-0028. The widest declared domain in the whole suite is `var 0..9`, so **no test can observe** the width-proportional cost D-0028 measures, nor M1-T25's eager `.opb`. Cheap witness, shape recorded in D-0028: two variables, one row infeasible on the declared bounds, zero prunings, w ~ 10^3 — about 24 kB of proof and 42 ms to verify. This is the project's signature failure mode (a cost no test can reach), which is why it gets its own row — Three `width_` models. `width_root_unsat` (w=999) is D-0028's shape: **zero prunings, zero decisions, 6 proof lines, one of them 23,779 bytes = 99.5% of the whole proof** — orchestrator confirmed. `width_narrow_unsat` (w=9) is the same shape at the suite's old width so the ratio reads off without running anything; `width_sat_depth` (w=99) is the Θ(w) *search-depth* cost D-0028 asks not be confused with it — near-identical proof size by an unrelated mechanism, which is what makes the tree column necessary. Gate +0.20 s (0.34 -> 0.54 s), reported not rounded away |
| M1-T31 | Remove the ambient model row: make `Linear.make`'s `?row_id` required, delete `Justify.emit`'s `Trivial -> ctx.model_id ()` arm | **DONE** | Found by M1-T27. `search.ml:323,338` pushes a decision with `Explanation.trivial`; `find_lo_reason`/`find_hi_reason` can return that entry, `summand_of_snap` wraps it as `Term (|a|, Trivial)`, and `Justify.emit` then renders it as the **ambient** row — which is not what established that bound. Latent, not observed: unreachable today only because a `Combine` is emitted solely at a root conflict (`offset_unsat` branches at every level and has 31 `rup`, **0** `pol` — orchestrator confirmed). Makes D-0011's original ambiguity a type error, the move D-0015 made for `Weaken`. **Before M4-T1**, and before D-0018 point 2's per-push `pol` path is first used — **Landed with M1-T50 as one change; they could not be split.** `Explanation.Trivial` deleted, `Decision of Lit.t` added, `Justify.ctx` loses `model_id`/`for_constraint`, `Linear.make`'s `?row_id` required. The ambient row is now **unrepresentable** (no field, no constructor) rather than guarded — the strong version the row asked for. `Justify.emit` on a `Decision` still raises, and the agent said so plainly rather than claiming the type-level form; the three routes to it all leave the file set (~74 test call sites). See **D-0037** |
| M1-T32 | State and enforce "`Compile` is the only door": check the arithmetic inside `Encoding`/`Opb` too | **DONE** | From D-0029. `lib/proof/encoding.ml` and `lib/proof/opb.ml` still compute unchecked and are safe *only* because `Compile` caps what reaches them. Nothing states that invariant, and `test_matrix.ml`'s own `build` already calls `Encoding` directly, so a second entry point can still write a corrupted row. A `Checked`-based assertion in `Encoding.linear_terms_int_lin_le` closes it. Not a live bug — an unstated invariant, which is how the first one got in — **Unblocked**: M1-T25 changed no code in `encoding.ml`, so the collision it was sequenced behind never happened — A local `Arith` guards `Encoding`'s two **committing** doors (`add_int_lin_le`/`add_int_lin_ne`), raising `Unrepresentable`. The copy of `Checked` is unavoidable (`lib/proof` cannot name `lib/core`) and is **pinned from the test layer, which depends on both libraries although neither depends on the other** — see **D-0036**, **I-X8**. The pure `expand_*` forms stay unguarded deliberately, so `test_prop.ml` can go on demonstrating the D-0029 defect: inspecting an expansion is not writing a file. All 87 artefacts byte-identical |
| M1-T33 | Make I-S1's oracle genuinely independent: `Model.check_assignment` must not share the propagator's arithmetic | **DONE** | From D-0029. It evaluates `sum a_i v_i` with the same wrapping `+`/`*` as the code it exists to check, so on the overflow class it agreed with the bug. Under the cap it cannot overflow today, so this is about the *value of the oracle*, not a live defect: an independent check that shares a failure mode with its subject is not independent. Consider arbitrary-precision or a different evaluation order — I-S1's linear sum is now exact (local `Exact` module in `model.ml`, sign+magnitude base 2^30, ~90 lines, no new dependency). Deliberately **not** reusing `Checked`: that is the propagators' arithmetic, and it answers a different question — it raises on overflow, while an oracle must return the right number. Still shares the model, the indexing and the existence of an answer, stated in the header rather than glossed. 174 artefacts byte-identical |
| M1-T34 | `bin/main.ml` should catch `Checked.Overflow` and exit with a diagnostic | **DONE** | From D-0029. Unreachable through the cap, so belt-and-braces only: if it ever fired the CLI would die on an uncaught exception rather than a positioned error. Small — Arm at `bin/main.ml:441`, **exit 4 (INTERNAL), not 3**: Compile's cap already refuses an over-large model with exit 3, so reaching the raise means the cap has a hole. Made reachable on *two* structurally different routes (inside `Search.solve` under `Fun.protect`, and before the proof channel exists) and watched red without the arm: `Fatal error: exception ... Overflow`, exit 2 — the same code as a usage error. Message tells the reader not to use that run's `.opb`, D-0029's asymmetry being that the row is already on disk |
| M1-T35 | A `--time` flag (or a library-level harness) so solve time is separable from process start-up | **DONE** | From M3-T5. The benchmark measures the CLI, and on 15 of 18 models process start-up swallows the entire signal — every timing there is `exec`. Until this exists, **no timing claim about the solver can be made from the model suite**, in either direction. This is the precondition for M3-T5 being able to answer anything about speed, including D-0026's prediction — `--time`, off by default, **stderr only**. CPU clock (`Sys.time`), forced: `bin/dune` does not link `unix`. Both candidates measured before choosing — `Sys.time` resolves to 1 us, `/proc/self/schedstat` only to 1.9 ms and cannot see a 400 us parse. **26 of 29 models are 84-96% `exec`**; the solver's own share is 0.37-1.6 ms, three orders of dynamic range the wall column hid. Orchestrator verified invisibility: 0 bytes on stderr without the flag, stdout/`.opb`/`.pbp` byte-identical with it |
| M1-T36 | A real node counter in `search.ml`, replacing the benchmark's `lvl` proxy | **DONE** | Closed 2026-09-17, whole rather than split — which is why it waited four waves. A **node** is one child dispatched by `branch` plus the root, counted **at the dispatch** rather than inside `dfs`, so the `Store.Conflict` arms of `explore_*` are included. Checked, not decorative: `stats_consistent` asserts `nodes = 2·decisions + 1` on an exhausted tree, under `Debug.check` in `solve` and on **every** run behind `--stats`, which prints `INCONSISTENT` instead of a number. `lvl` differs **structurally**, not by a constant — a marker per child *and* per step back down, none without `--proof`, and `width_root_unsat` reads 0 markers for a root that was visited and refuted. `width_sat_depth`: 99 nodes / 49 decisions vs 196 markers. `bench` keeps `lvl` beside `nodes` and its verdict now distinguishes **proof-only** from CHANGED, which is the D-0026 case the proxy could not express. One break reddened **nothing** and was reported as correct: counting at `dfs` entry coincides in every reachable execution, because those conflict arms are unreachable under I-D2 | From M3-T5. `lvl` counts level markers in the proof as a stand-in for nodes and is labelled a proxy everywhere it is printed. D-0026's claim is about cost *at the same search tree*, so a benchmark that cannot say whether the tree changed cannot test it. Small |
| M1-T37 | The `.opb` header embeds the input path, so its byte count depends on where the file lives | **DONE** | From M3-T5. `* baguette: <path>` means the same model measured as `test/models/x.fzn` and as `/tmp/x.fzn` differ by ~16 bytes. Harmless inside one comparison, but it makes two people's byte counts legitimately disagree, and `.opb` bytes are the one column M3-T5 found trustworthy at every row. Trivial — Header is now `Filename.basename`, kept rather than dropped because someone holding a `.opb` and `.pbp` in a temp directory still needs to know the model, and the directory is the part they already know. Orchestrator verified: the same model through an 89- and a 144-character path yields byte-identical `.opb` (2239 B both) |
| M1-T38 | Replace `root_unsat` with an instance whose model rows are each individually satisfiable | **DONE** | From D-0030. `root_unsat`'s `@c2 +1 ~x_ge_2 +1 ~x_ge_3 >= 3` is **infeasible on its own**, so the model is refuted by one row and every derivation above it is decoration — orchestrator verified that a proof containing *no derivation at all* returns `s VERIFIED UNSATISFIABLE` on it. Its `truncate-derivation` lane is therefore ACCEPTED and registered `Known_slack`. Its `pol-coeff`/`pol-cite` lanes reject only because the `conclusion` hint names the row the corruption breaks — a weaker claim than they have been read as making. Needs a model where no single row refutes — `triple_unsat` replaces `root_unsat`, certified **two ways** — arithmetically, and by the checker rejecting a derivation-free proof for every row id. The `Known_slack` registration came off only because the replacement actually rejects. `z` appears in no other row and that is load-bearing: without a spectator variable the derivation is all ids and no coefficients, and `pol-coeff`/`perturb-coefficient` have **no site** — measured on the two-variable version first, where both lanes reported not-applicable. `root_unsat` stays as the **control** for the new `rows_that_refute_alone` procedure. Mutation checks 37 → 44 |
| M1-T39 | Delete `Writer.pol_raw`, or document why it stays | **DONE** | From M1-T26. It has **no caller anywhere in the tree** and no mutation knob, so it is the one emission path the harness cannot corrupt. Either is fine; leaving it undecided is not — Deleted. It bypassed `corrupt_pol`, so it was the one `pol` path the mutation harness could not reach — D-0030 makes a knob incapable of firing in a normal run, and the dual obligation is that **no emission path is structurally incapable of being corrupted** |
| M1-T40 | `test_compile.ml`'s `evaluate` oracle says `int_lin_ne`/`int_ne` are "rejected by compile" — stale since M1-T11 | **DONE** | Found while making the M2 Boolean branch build. It is a loud `failwith`, not a silent wrong answer, so it cannot launder a bug; but it means that oracle has **never** been exercised on a disequality, and if one ever reaches it the message misdirects. Give it real semantics **together with a test that reaches it** — writing an oracle branch nobody has seen run is what D-0030 is about — `evaluate` has real `Int_lin_ne`/`Int_ne` semantics written from the FlatZinc definition, plus three models that reach them — including `int_ne(x, x)`, UNSAT at the root, where an arm reading `=` cannot hide behind picking a different witness. Each arm inverted in turn dies with `Search.Unsound_solution`, so the branch is demonstrably reached |
| M1-T41 | `test/unit/test_proof.ml:316` pins `consistency_id`'s *eager* value, so it is the first test any future lazy-ladder attempt breaks | **DONE** | From M1-T25/D-0031. It asserts `consistency_id e "x" 1 = Some 1`, which becomes `None` before materialisation under any lazy scheme. It should assert the **transition** — `None` before, the rung's id after — so it stays meaningful under either policy. Cheap, and it makes D-0031's rejected design testable rather than merely argued — Now asserts the **transition** rather than the eager value: a declared variable with no rung has no id, every interior rung has one after materialisation, distinct rungs get distinct ids, bounds never get one, and an undeclared variable still **raises**. Meaningful under either ladder policy, so it is no longer the first test a lazy-ladder attempt breaks |
| M1-T42 | `test_prop.ml`'s **integer** veripb builders hold their setup facts as `.opb` model rows | **DONE** | From D-0032. `build_int_lin_eq_multi`, `build_int_le_multi`, `build_int_lt_multi`, `build_int_eq_multi` and the `int_ne` builders all do what the Boolean scenes did — and there, it made a justification with **no facts at all** pass every check. Whether it weakens these depends on each row's shape and is **unmeasured**; measure before editing, and add a factless control per propagator. A blind edit would be a guess — **Measured before changing anything: 8 of 9 veripb builders accepted a justification with its facts deleted.** The row assumed one cause; there are two. The three `int_ne` scenes are D-0032 (facts held as `.opb` model rows, so a factless `rup` is *true* and veripb is right to accept). The five `pol` builders are weakened by something stronger and unfixable by moving facts: **a `pol` states a derivation, not a claim**, so the checker recomputes it and accepts any well-formed expression — a factless control on a `pol` is impossible. `build_ne_conflict`, the only scene that never posted a pin, was already sound. Facts now live in the store; six new factless controls are each REJECTED by veripb |
| M1-T43 | `bool_reif_unsat` does not catch a literal-polarity flip that five SAT models do catch | **DONE** | Closed 2026-09-17 by **performing the flip** rather than relying on M2-T1's report, in a throwaway worktree because `bool_clause.ml` was held by another session. Confirmed, with the count corrected: swapping `Lit.bool_true`/`Lit.bool_false` in `pb_lit` leaves `bool_reif_unsat` reporting `s VERIFIED UNSATISFIABLE`, and **six** SAT models catch it, not five — `bool_and_sat`, `bool_array_sat`, `bool_clause_sat`, `bool_eq_sat`, `bool_not_sat`, `bool_or_sat`. Three further Boolean models also miss it (`bool_channel_sat`, `bool_channel_unsat`, `bool_out_sat`), which is expected: the channelling pair route through `bool2int`, not through this `pb_lit`. **The generalisable lesson, now in the model's header**: an UNSAT model proves a refutation exists and a wrong encoding can still admit one, whereas a SAT model must produce an assignment the model itself accepts (I-S1), so a wrong encoding has nowhere to hide. For an ENCODING bug reach for a SAT model; for a REFUTATION bug reach for this one. No code change — the model is not defective, and its header now says what it is not a net for. Artefacts byte-identical (the `.opb` carries the model name, not its comments) | From M2-T1. Flipping every `pb_lit` polarity turns some of its reasons into clauses the reified structure happens to entail. Not a defect — the model's job is the D-0030 single-row property and the two-level refutation — but it means the UNSAT model is not a universal net, and the next person may assume it is |
| **M1-T44** | **A correct UNSAT answer whose proof the checker rejects (I-X7).** Reproduced by the orchestrator through the CLI | **DONE — fixed, and this row was stale until 2026-09-18** | Found by M2-T11's sweep at seeds 133 and 151, and it **predates M2-T11**: with `search.ml` reverted the `.pbp` is byte-identical. Not a branching bug — the conflict is at the root, before any order decides, and all nine orders emit the same proof. Minimal model: `var -1..1: v0; var 0..2: v1; int_le(v1,0); int_ne(v0,v1); int_eq(v1,v0);`. Answer `=====UNSATISFIABLE=====` is **correct**; every derivation step is **valid** (`conclusion NONE` gives `s VERIFIED NO CONCLUSION`); the one false thing is the claim that `@c10` is a contradiction. The chains cancel completely and it lands on **`0 >= 0`** — one unit short, the shape the M1-T16 handoff predicted, reached with no mutation. Both checkers agree, so it is not a format quirk. Cause: `dfs`'s root arm uses `Search.rests_on_a_clause` as its discriminator, but the right question is whether the derivation *is* `0 >= k` with `k >= 1`. Pinned as `xreject` in `test_random.ml`; gate is green **Closed, and the fix was not the one this row proposed.** The row blamed `dfs`'s root-arm discriminator and asked for "is the derivation `0 >= k` with `k >= 1`". The actual cause, found and fixed by a later session from the `test_random.ml` end, was upstream of that choice: **a bound settled over a hole is stronger than the trail entry carrying it**, so the citation was one unit short and the hole's own `Clause` reason was dropped from the derivation (`lib/core/prop/linear.ml`, `settled_over_lo`). `rests_on_a_clause` was never the defect. **Re-verified end-to-end by the orchestrator, 2026-09-18**, through the CLI on this row's own minimal model: `s VERIFIED UNSATISFIABLE` from veripb 3.0.2, binary `0011dc99`. The proof no longer rests on the `pol` chain being a contradiction at all — it closes with `@c15 rup >= 1`, cited by `conclusion UNSAT`. The `xreject` pin is gone from `test_random.ml` (which says so at line 791) and the instance is `test/models/root_hole_unsat.fzn`, **byte-for-byte this row's minimal model**, so the whole gate now runs it against the checker every time rather than one test file. |
| M1-T45 | Reclaim the 0.4% of branching shapes `random_order`'s hole guard refuses | **DONE (guard removed on evidence)** | Closed 2026-09-17. The guard is **gone** — three draws per decision, no retry, no `pick`. The row's bar was "taking those shapes back needs an instance showing what it catches", and **no such instance was found**: `test_hole_split_sweep` forces **40** previously-refused shapes (eight interior hole patterns of a width-5 domain × every refused split × both child orders, counted against the 12 the guard allowed, both pinned) and runs 3.0.2 over each proof — **0 rejections** — plus a depth-2 case with a settled hole in `bridges`' ancestor list. The sweep is sensitive: corrupting its own proofs turns all 40 into rejections. What changed since the row was written is **M1-T55**, which put the missing implication on the page, so the reason for the guard was gone. See **M1-T66** for the finding that came out of it | From M2-T11. The guard was found disabled (`if true \|\| ...` — short-circuit made the test, the recursion and `tries` all dead) and restored. Measured what it costs: **2,051 of 495,723 splits** over 108,000 runs land where it now refuses, and **none was rejected** by veripb. So the guard is conservative, not measured-necessary. Taking those shapes back needs an instance showing what it catches. Note the guard is not total anyway: `pick`'s fallback after 8 misses can itself land where it refuses |
| M1-T46 | Comments in `ne.ml` (and two test files) still quote veripb **2.2.2**'s wording | **CLOSED 2026-09-18 — dissolved by D-0046 / M2-T16.** The rule this row enforced ("never match on one checker's wording alone") existed because there were two checkers wording rejections differently. There is one, so matching its wording *is* correct, and every site was simplified deliberately rather than left carrying a comment about a binary that is gone. **What survives of this row, and it is the valuable half**: a lane asserting a rejection must assert the checker's WORDING at full strength, not a non-zero exit and not a fragment weaker than its claim — an exit status cannot tell a JUDGEMENT from a parse error (M2-T14). That is now stated in `CLAUDE.md` and `PROOF-FORMAT.md` §2a in place of the old rule. | From M2-T11. They say `"Constraint is not a contradiction"`; 3.0.2 — emitted and checked by default since D-0025 — says `"... is not contradicting, as specified by the hint"`, sharing no substring. **Correction (orchestrator, 2026-09-16): this row named `PROOF-FORMAT.md` and `DECISIONS.md` wrongly.** `grep` finds no such wording in `docs/PROOF-FORMAT.md` at all, and the one hit in `docs/DECISIONS.md:905` is inside D-0016's record of what a checker said *at the time*, which append-only discipline says to leave alone — a decision log records what was true when written. The live matcher in `test_random.ml` was **already fixed** and now matches both wordings at both sites with the trap explained. `lib/core/prop/ne.ml` **done 2026-09-16**. Remaining, both in claimed files, routed as cross-session requests: `lib/core/search.ml:260-261` (agent-rootfix) and `test/unit/test_proof.ml:1125,1150` (agent-proofhyg); `test/unit/test_matrix.ml:1673` is free and unclaimed |
| M1-T47 | Separate proof *emission* time from propagation time | **DONE** | **The row's own hook was wrong and the agent corrected it rather than following it.** "One accumulator at `writer.ml:402`" is true of rules and false of the file: `comment`, `always_comment` and `conclusion`'s `flush` all bypass `line`, and under format **3.0 — the default — every level marker goes through `always_comment`** (`if v3 t then always_comment ... else line ...`, orchestrator confirmed at the source). Instrumenting only `line` under-counts by **46%** on `width_sat_depth` with no symptom. A second trap found by measuring: a timer around `line`'s *body* misses the write, because `Printf.fprintf` returns a closure that consumes the remaining arguments after `line` returns — it reads 18% low, not zero, which is worse. Uses `Printf.kfprintf`. **Result: emission inside `search` is small** — 7.7% of `width_sat_depth`, 2.3% of `width_root_unsat`; the proof is still the expensive half (39% of `inmain`) but M1-T35 had already separated nine tenths of it. `emit` is published as a **lower** bound and `propag` as an **upper** bound, with the instrument's own cost (`clockovh`, 43% of `emit` on the heaviest model) as its own column rather than folded in. Artefacts byte-identical on all 90 hashes; accumulator gated off by default at 1.2–1.6 ns/line |
| M1-T48 | `--proof-comments` is a no-op on all 29 models — untested by construction | **DONE (the text half)** | From M1-T35. `Writer.comment` has exactly three callers, all in `encoding.ml`'s direct-encoding path (M4), which no current model reaches. Found while trying and failing to break the `.pbp` self-check with the flag. Not a bug today; it becomes one the moment M4 lands and nothing is watching. **Confirmed on all 30 models by M1-T47**: every `.opb` and `.pbp` is byte-identical with and without the flag, on the pre-change binary too, so it is pre-existing. **And the CLI usage text over-promises it** — it advertises "annotate the proof with the step that produced each rule — large", which is not what it does. Either make it do that or say what it does  **Closed 2026-09-17 as the text half only, and the row says so rather than claiming more.** The usage text now states the flag is a no-op on every shipped model and why; `test_endtoend.ml`'s `test_proof_comments_noop` pins `Writer.comment`'s suppression mechanism (the live control — it reddens when broken) and compares `.pbp` bytes with and without the flag (**prospective only**: it cannot differ until a reachable path emits a comment, so it is a signal for M4, not evidence today). Making the flag do what the old text promised needs `Writer.comment` wired into `justify.ml` and the propagators — see the standing request in `WORKLOG.md`; it is M4's job ||
| M1-T49 | The gate cannot see "timings are off unless asked" | **DONE** | From M1-T35. With timings emitted unconditionally, `scripts/run_model_tests.sh` still reports 29/29, because it diffs **stdout only** and the report is on stderr. The bench self-check is the sole thing watching that property and the bench is deliberately not in `make check`. Either teach the model harness to assert stderr is empty, or accept the gap explicitly |
| M1-T50 | `linear.ml` cites its **own row twice** when the fact came from a decision | **DONE** | From M1-T42. A decision pushes explanation `Trivial`; `snapshot_source` takes the `Snap_cite` branch, `summand_of_snap` emits `Explanation.term 1 Trivial`, and `justify.ml:288` answers `Trivial -> ctx.model_id ()`. The `Combine` therefore emits **`pol <own row> <own row> +`**, deriving twice the propagator's own row where the fact should be. Measured: `decision_scene_int_le` emits exactly `@c11 pol @c9 @c9 + ;`. **Orchestrator confirmed independently by reading `justify.ml:288` and `linear.ml:317,352`.** veripb accepts it because a `pol` has no claim to check, and it looks harmless today only because `search.ml` discards that id and closes the branch through the trace lines and the nogood. Possible lead on M1-T44, whose `pol` chain cancels to `0 >= 0` — **Fixed as a mislabel, not a rendering bug**: "the model constraint justifies this" is false of a decision. `Snap_assume` weakens the term out of the row while still contributing its bound literal to the trace line. **The blind spot it exposed is the lasting result**: pushing the *wrong* decision literal left the whole suite green — nothing read it — and `test_matrix.ml` now checks at every level start that the reason names the variable that moved, in the direction it moved, with the recorded bound entailing it. Matrix checks 239 → 252; zero of 30 artefacts change |
| M1-T51 | Nothing distinguishes a `pol` that derives the claimed bound from one that derives something weaker | **DONE (mechanism); adoption blocked on D-0038** | **The premise of this row was half wrong, and that is the finding.** It said a checker-level control "needs an `e`/`ia`-style rule in `lib/proof/writer.ml`". The *rule* was never missing: `ia` and `e` exist in **both** 3.0.2 and 2.2.2, and `docs/PROOF-FORMAT.md` §2a simply did not list them — now corrected, with all four rejection wordings measured and a trap subsection, because a **misplaced hint fails open**: `ia C ; @NOPE` verifies clean, since after the `;` a label belongs to the next rule. `Writer.implied` (hint mandatory — unhinted, an `ia` can be satisfied by a ladder clause rather than by the derivation above it) and `Writer.pol_concluding` (returns the *claim*'s id, so a caller still owes exactly one deletion and I-X2 reads unchanged) landed. **The break was performed on the production writer**: a `pol` truncated to derive something strictly weaker is ACCEPTED bare and REJECTED once the conclusion is stated; the old shape pins caught neither, since they never run against a corrupted writer. **But there is no caller in `lib/`**: `Explanation.Combine`/`Cut` carry no conclusion, so the claim does not exist to pass — that is **D-0038**, four routes costed, +2% proof lines if adopted everywhere. Also measured: the suite emits only ~10 `pol` lines in total, all where the `pol` feeds the cited contradiction, so today's coverage says nothing about a pruning the conflict never cites — exactly what M2-T3 creates | From M1-T42. This is the structural reason M1-T42 could only reach 8 of 9 with text pins: `Writer` exposes no rule that states what a step *concludes*, so there is no checker-level control on a `pol` at all. Until one exists, every `pol` guard in the suite is a shape pin, and the code says so. A checker-level control needs an `e`/`ia`-style rule in `lib/proof/writer.ml` |
| M1-T52 | `Model.check_assignment`'s tests are split across two files | **DONE** | Moved 2026-09-17; check-neutral, 145 -> 145. From M1-T33. The new `Exact` tests are lodgers in `test_compile.ml` because `test/unit/test_output.ml` belonged to another session that round. Move them together when both files are held at once; the code marks them as lodgers |
| M1-T53 | A bare `dune runtest` still bypasses the memory cap | **DONE (12 of 15 binaries)** | **Closed 2026-09-17 via the `Gc.create_alarm` route the row preferred.** `test/unit/mem_guard.ml` aborts naming the binary and the MB crossed; its header documents what it cannot see (anything off the OCaml heap — a `Bigarray` or a C stub's malloc), so `ulimit -v` remains the outer backstop and the two are complementary, not substitutes. **Two corrections the orchestrator made to its own dispatch:** the first landing covered only test_output/test_compile and so missed `test_prop.exe`, the binary that actually reached 14.9 GB; and `MEM_GUARD_DEMO` installs a guard of its own, so it never tested the *installed* one — at `BAGUETTE_TEST_HEAP_CAP_MB=1` only test_prop and test_output have heaps large enough to abort, leaving arming unfalsifiable for the other ten. `BAGUETTE_TEST_HEAP_CAP_ANNOUNCE` now makes arming directly checkable (12/12 announce). test_engine/test_proof/test_trace get their line as their sessions release. Wrapping the `(tests)` stanza is the route NOT taken and stays available. `make`, `scripts/run_model_tests.sh` and `scripts/verify_proof.sh` now apply `ulimit -v 4000000` themselves, and the cap is verified to bite (a 5 GB allocation dies with `MemoryError`; 100 MB passes; the gate runs at 18.5 MB peak). **But agents in worktrees run `dune runtest --root .` directly, which is exactly how all three OOM incidents happened**, and that path is capped only by CLAUDE.md telling them to wrap it. Closing it properly means either wrapping the `(tests)` stanzas in a capped runner — `test/unit/dune` is a contention hotspot, so do it when nobody else holds it — or installing a `Gc.create_alarm` heap guard in a shared test helper that aborts past a threshold. The alarm is the more honest fix: it fails the test rather than the machine |
| M1-T54 | A width cap in `Encoding.declare_int`, so a wide domain is a loud refusal | **DONE** | Closed 2026-09-17 as **D-0041** + a normative SPEC §3.1 paragraph, which supersedes D-0028 part 3 / D-0031's "no such limit exists, and that is deliberate". `Encoding.max_order_width = 10_000` on `hi - lo`, two layers one constant: `Encoding` raises `Width_too_large` before the Hashtbl and before the ladder loop so a refusal allocates nothing, `Compile` carries the positioned diagnostic and **exit 3**. Deliberately **not** `Unrepresentable`, whose arm exits 4 and tells the reader baguette's invariant failed — misleading for a legal FlatZinc model. Overflow-safe by construction: `min_int..max_int` has width 2^64−1 and a naive `hi - lo` computes −1 and would accept it. **No escape hatch**, argued in D-0041 point 3 — a knob would make the accepted language environment-dependent. 10× headroom above `width_root_unsat`'s deliberate w=999. `bin/main.ml` gained the matching exit-4 arm. Follow-ups filed: **M1-T65** (the cap is per variable, so a model's total ladder is still unbounded) and **M1-T64** | **D-0028 point 3 now has the evidence it was waiting for**: three memory-ceiling incidents in one day, all the same shape. `scripts/check_test_widths.py` catches the *syntactic* form in the gate (and its self-test runs first, on every gate), but it cannot see a width built at runtime or passed through a variable. A real cap refuses the model instead. **This is normative — it changes which models the solver accepts — so it needs a SPEC change and a decision record, and D-0029 explicitly declined to make its arithmetic limit a width cap.** Not to be decided in passing |
| M1-T55 | `spec_order` can decide at a hole; `random_order` deliberately will not | **DONE** | **Closed 2026-09-17 by a route the row did not name, because both routes it did name are unavailable.** Guarding `spec_order` changes *where it splits*, and that is SPEC §3.4's `indomain_min` itself; and making the trail entry land on its literal is **impossible, not merely costly** — the low side lands exactly iff `k` is in the domain, the high side iff `k+1` is, and demanding both *is* the guard condition (taking the literal from the settled bound only moves the gap to the low side, where the two children's nogoods then no longer resolve on one literal). So `Search.bridges` states the missing step: per settled decision, `rup +1 x_ge_m +1 ~x_ge_b +1 ~<ancestors> >= 1`, emitted after `Trace.emit` (D-0021) and before the nogood, at the nogood's level so the same `w` retires it. **Tree unchanged** — all 30 pre-existing `.opb`/`.pbp` byte-identical, measured by regenerating with and without. **No instance exhibits the defect, and the structural reason is the finding — see M1-T60.** Not normative, but a SPEC §3.4 sentence is proposed in the handoff. From M1-T44. `spec_order` splits at `d_split = lo` and `explore_ge` pushes `set_lo v (lo+1)`; `lo+1` may be a hole, so the decision's trail entry lands on a bound strictly stronger than the `x_ge_(lo+1)` its nogood negates — exactly what `random_order`'s restored guard refuses, in the **normative, CLI-reachable** order that has no such guard. M2-T11 measured 2051 such splits with zero rejections, so this is a latent asymmetry rather than a live defect. The branching rule is SPEC 3.4, so closing it is normative |
| M1-T56 | `trace.ml`'s `claims` writes no line for a pure hole removal | **DONE** | Closed 2026-09-17, and it is **M1-T57's prerequisite rather than its sibling**. The blocker was a false premise, not a missing mechanism: a claim was assumed to be a single order literal and never had to be — `x <> v` is the two-literal clause `x <= v-1 or x >= v+1`, i.e. `Encoding.ne_clause_lits`, which PROOF-FORMAT §4 already named as `int_ne`'s justification. A claim is now a clause and `claims` is driven off `Domain.classify`, so `Bound`-or-`Holes` disjointness comes from the module that owns it. The same stale "a hole gets no line" prose was sitting in **three** places (`trace.ml`, `search.ml`, `store.ml`); all three are corrected. From M1-T44. The comment says "correct today (M1 is bounds-only)"; `int_ne` has punched holes since M1-T9/M1-T17, so a hole's `Clause` reaches the proof only if something later cites it. M1-T44 made the *root-conflict* path cite it; a hole whose reason is never cited is still invisible to the checker. Same shape of stale comment as the `store.ml` one M1-T16 caught |
| M1-T57 | A settle-strengthened bound makes its own trace line stronger than its facts | **DONE** | Closed 2026-09-17. The line now also cites the facts of the holes the settle walked over (I-X9, D-0035), recovered from `e.old`/`e.now` plus a backward trail scan for each hole's remover, which makes it RUP against those holes' own M1-T56 lines. On `root_hole_unsat` the pre-fix line claimed `v0 >= 1` with an **empty** tail — unconditional, I-P5's worst case, and false. **Why ~111k runs saw nothing**: that model is UNSAT and the checker re-derived the hole from the big-M rows unaided. The two new SAT models are what make it observable, because there the pre-fix line is *violated by the model's own solution*. Breaks reddened 7 (T57), 4 (T56) and 9 (both) unit checks and 2 of 34 model tests, with veripb **rejecting**. **Forced D-0039** (a trace line is RUP in sequence, not standalone) and **I-S4** (a cited hole line must outlive the line citing it). From M1-T44. `claims` reads `e.now` (the settled bound) while the facts come from the propagator, so the line claims more than the facts justify. It is a `rup`, so it fails loudly rather than silently, and it verified across ~111k runs — but only because the checker re-derives the hole from the `.opb` disequality rows by unit propagation, which is **not guaranteed in general**. Same root phenomenon as I-X9 |
| M1-T58 | `bin/main.ml` does not catch `Encoding.Unrepresentable` | **DONE** | Closed 2026-09-17 by the orchestrator, mid-wave-seven, because M1-T54 depends on it: a width cap at `Encoding`'s committing door is only a "loud refusal" if something catches it. **Measured rather than assumed** — the raise was injected into `solve` on the pre-fix binary: `Fatal error: exception Baguette_proof.Encoding.Unrepresentable(...)`, **exit 2**, the same code as a bad command line, with no context and no warning that the `.opb` may already be on disk encoding a different model than the `.fzn`. Now a positioned diagnostic at **exit 4**, by `Checked.Overflow`'s own argument (a model genuinely over the limit is rejected by `compile.ml`'s cap at exit 3, so reaching here means the cap has a hole — this binary's invariant failing, not a model problem). One thing it says that Overflow's arm does not, and I-X8 is why: `Encoding` raises on behalf of **any** caller and only `bin/main.ml` goes through the cap, so reached through a different caller the exception is the documented contract rather than a bug — the message distinguishes the two | From M1-T32. `Checked.Overflow` gets a positioned diagnostic and an exit code; a row refused at `Encoding`'s committing door would surface as an uncaught exception. Unreachable under Compile's cap today — but a second front end is exactly the caller I-X8 exists for |
| M1-T61 | The I-X10 closure gate: make a new propagator family a visible event | **DONE** | Closed 2026-09-17 by the orchestrator. Seven checks in `test/unit/test_trace.ml`, two kinds: **closure** reads `lib/core/prop/` at test time and asserts every module is classified `Single_row`/`Needs_derivation`/`Prunes_nothing`; **content** asserts 3.0.2 actually refuses the Hall bound move `rup +1 z_ge_5 >= 1` on a satisfiable saturated scene, with an accept-side control on the same `.opb`. All three breaks performed — a `touch`ed `all_different.ml` reddens closure, a line measured VERIFIED reddens the refusal assertion, and `$VERIPB` pointing nowhere reddens both lanes. The unused-constructor warning on `Needs_derivation` is **answered rather than suppressed**: a check asserts no family needs a derivation today, so M4-T1 classifying itself reddens on purpose — this gate sees a family arrive but does not verify the `pol` is emitted, and the red line is how its author learns they owe the stronger check | **What converts I-X10 from argued to checked**, and the half of M1-T60 that is not done. Two parts, per agent-property's design. **(a) Closure**: a table in `test/unit/test_trace.ml` mapping every module in `lib/core/prop/` to `Single_row` or `Needs_derivation`, asserted **exhaustive against a directory listing read at test time** — OCaml cannot reflect over modules, and the cwd-relative read is the mechanism `test/models/` already uses, so `--root .` covers it. `touch lib/core/prop/all_different.ml` must then redden the suite naming the unclassified module. **(b) Content**: a `hall_sat.fzn` model (`x,y,w in 2..4`, `z in 2..5`, six `int_ne` — widths 3 and 4, well inside D-0028) plus an assertion that `standalone` on `rup +1 z_ge_5 >= 1` returns `Some false`. `test_trace.ml:345` already has the `standalone` primitive and already runs both polarities. **The model MUST be satisfiable** — `test_trace.ml:499` says why, and M1-T57's defect surviving 111k runs on an UNSAT model is the proof that it matters. `None` (checker absent) must be a **failure**, per `scripts/checker.sh`'s no-skipping rule. Break recipe for (b): swap the asserted line to `rup +1 ~z_ge_4 +1 z_ge_5 +1 ~x_ge_4 >= 1`, which is measured VERIFIED on that `.opb`, so a must-be-refused assertion has to go red |
| M1-T62 | `Store.remove` and `Store.fix` have no `lib` callers and silently record `no_facts` | **DONE (closed by M2-T8, not by separate work)** | **Both halves of the premise died on 2026-09-17 and I verified it rather than dispatching the row.** `no_facts` does not exist any more (0 occurrences in `store.ml`), and all four mutators now read `let remove t v value j = apply t v ... j` — one `Reason.justified`, with the code saying "there is no factless sibling and no optional argument". So the **silent** path the row complained about is closed by the type, which is exactly the third option it named: make `no_facts` impossible to reach from `lib/`. `Store.remove` also has a real `lib` caller now (`ne.ml:343`), so "no `lib` callers" is false too. **What remains is deliberate and visible**: `Reason.none` exists and is used in three commented places — a decision (which by D-0037 rests on an assumption and has no facts), an all-fixed `int_ne` violation, and the conflict construction — and each says so at the call site. The row wanted the silent route gone, not the explicit one, and the row's own caveat about tests needing the factless shape is satisfied: they now write `Reason.none` and are seen writing it |
| M1-T63 | Two hedges for a declared hole are unreachable dead paths | **DONE** | Closed 2026-09-17, and **the row's `linear.ml:229` citation is struck: M2-T8 deleted that paragraph wholesale** (it was the `find_lo_reason`/`find_hi_reason` header), so there was nothing left to keep or drop. The other two halves of the premise still held and were verified: `compile.ml:385`'s `reject_set_domain` refuses `Model.Dset` unconditionally and `Domain.of_list` has **no caller anywhere in `lib/`**. The hedge now survives in exactly **one** place, `Store.remover`'s header, which already names `reject_set_domain` as the gate — i.e. this row's "keep it and say it is unreachable today" was already satisfied, in one copy instead of three. A note at `lo_rests_on` stops the next reader reinstating the deleted paragraph. `trace.ml`'s copy was handed to agent-index as a request |
| M1-T64 | `make fmt` auto-promotes, so the gate silently rewrites instead of reporting | **DONE** | Closed 2026-09-17. `check` depends on a new `fmt-check` target that runs `dune build @fmt` **without** `--auto-promote`; `make fmt` keeps `--auto-promote` and is the explicit fixer. `scripts/check_fmt.sh` carries the check and a `--self-test` that runs **first on every gate**, the discipline `lint` already follows. The self-test builds a **throwaway dune project in a temp dir** rather than writing a deliberately unformatted module into this checkout — two other sessions share the tree, and a stray `.ml` in `lib/` is their broken build, not just mine — and it asserts **both** polarities, because a check that always fails says as little as one that always passes. Measured both ways: with an unformatted line added, `make check` exits **2** with `FAIL fmt` and **leaves the file alone**, where `make fmt` silently rewrote it to `let _m1t64_probe x = x + 1` and would have passed. A missing `ocamlformat` is a **loud skip, not a failure**, and the asymmetry with `scripts/checker.sh` is deliberate and commented: veripb verifies a correctness property so a missing one makes the run worthless, ocamlformat verifies a cosmetic one so refusing to gate without it would block real work over whitespace | Found by agent-widthcap. `Makefile:27` runs `dune build @fmt --auto-promote`, so an unformatted commit is **silently reformatted by the next person's gate run** rather than failing anybody's. Two consequences, both observed on 2026-09-17: M1-T53 committed two test files unformatted and nothing said so; and the orchestrator then committed that formatting debt as a side effect of an unrelated M1-T58 commit, because sessions are told to stage explicit paths and the reformatting showed up in `git status` looking like their own work. **Two further sessions independently reported the same files as unformatted**, each seeing its own branch base — wasted attention that a failing gate would have spent once. The gate should report and refuse; `make fmt` can stay as the explicit fixer. Note the interaction with M1-T54's lint: ocamlformat and `check_test_widths.py` actively disagree about one line, so whichever way this goes, that pair needs a test |
| M1-T65 | The width cap is per variable, so a model's total ladder is still unbounded | **DONE (declined — D-0042)** | Measured 2026-09-17 and **declined**, in the register D-0028 part 3 used for the per-variable cap and with the trigger stated. The mechanism D-0041 filed is real and is now quantified: `.opb` cost is **≈40–58 bytes per unit of TOTAL declared width**, and four shapes summing to 10 000 land within 1.6× of each other, so the total governs and the per-variable cap bounds one term of the sum. But a limit is not due. **Total 100 000 is a 4.4 MB `.opb`, ~1 s solve, ~0.3 s verify** — D-0028's condemned case is 156 MB, a factor of 35 away. And reusing D-0041's 10 000 as an aggregate would **refuse ordinary models**: 1 000 variables declared `0..10` totals 10 000 and yields 367 kB in 142 ms. All three observed incidents were single wide domains, which D-0041 already catches; reaching the aggregate gap needs ≥100 variables at the per-variable cap, which nobody writes by accident. The suite's widest model by total is `width_root_unsat` at **1 998**. D-0042 records the table so whoever reverses this does not re-derive it, and names four triggers | Filed by D-0041 rather than decided there. `max_order_width = 10_000` caps each declared domain, but 1 000 variables at w=9 999 each still produce the artefact the cap exists to prevent. An aggregate budget is a **different** design question, not a bigger constant: it has to name a variable to blame for a total that no single variable caused, which is why D-0041 declined to settle it in passing. Normative if it lands, so it needs its own SPEC paragraph and record. Note D-0041 point 3's argument against a run-time knob applies here too |
| M1-T59 | Two proofs emit a byte-identical `rup` twice | **DONE** | Closed 2026-09-17, and it **fell out of M2-T9 with no further work**, exactly as this row predicted. `root_hole_unsat` `@c14 -> @c9`, `bool_channel_unsat` `@c8 -> @c6`; 2 of 102 artefacts moved, both `.pbp`, verified by the orchestrator. Note what was deliberately *not* done: `emit_rup_clause` populates the index but does not consult it, because the trace is the primary record and consulting there would make `emitted_ids` list an id twice and hand `Search.solve` an id to delete twice — an **I-X2 violation rather than a saving** | Found by agent-trace3 on `root_hole_unsat` (`@c9`/`@c14`), where M1-T44's root-conflict path re-derives a line the M1-T56 trace already wrote. **The orchestrator then scanned all 34 proofs and found a second one nobody reported**: `bool_channel_unsat` emits `rup +1 ~x_ge_2 >= 1 ;` at lines 3 and 5. Harmless today — a duplicate derives the same constraint, gets its own id, and both are deleted, so I-X2 still balances and the audit passes. It is two wasted lines out of ~960. **It stops being harmless per-pruning rather than per-proof**, which is what M2-T3's learned clauses create, so measure before assuming the scale. The fix is a question about who owns the claim: `justify.ml`'s conflict path does not know `trace.ml` has already stated the fact, and M2-T9's literal -> defining-proof-line index is exactly the structure that would tell it |
| M1-T60 | The proof rests on an unstated property of the current propagator set | **DONE (stated); gate is M1-T61** | **Verified 2026-09-17, and my own framing of it was wrong in a way that mattered.** The property is real: every M1 pruning follows from a **single model constraint**, whose rows unit-propagate the claim once the line's own facts are assumed. Stated as **I-X10**, with **D-0040** for its consequences. Enumeration (re-checked by the orchestrator against master): `ne.ml:327` is the **only** `Store.remove_with_facts` caller in `lib/`; `Store.remove`/`Store.fix` have **no** `lib` callers at all; `settle` only walks past existing holes; `Domain.of_list` is unreachable because `compile.ml:337` rejects `Dset` outright. **The correction**: I wrote that Hall pruning "removes values with no disequality row behind them" — it removes nothing, it pushes a bound out of a saturated interval, which is what *bounds* consistent means. So an invariant phrased about **holes would have let M4-T1 through silently**, the exact failure this row exists to prevent. Measured by the orchestrator on a satisfiable 4-variable Hall scene: the Hall **bound move** `z_ge_5` is refused, Régin's hole `z<>3` is refused, an `int_ne` line on the same `.opb` verifies, and both pruned values are entailed (`z` in `2..4` UNSAT; `z=3` UNSAT). See D-0040 for the second correction — **M4-T1 does not force the direct encoding**, only an explicit `pol` ahead of its trace line |
| M1-T45 | (evidence added 2026-09-17) | — | agent-search3 measured `Search.bridges` with `random_order`'s guard **temporarily lifted**: 108 bridges over 16,000 runs (10 seeds x 200 cases x 8 orders), **0 rejections**. That shows the bridge is exercised and does not hurt; it does **not** show the guard was unnecessary, which is still what M1-T45 needs. Not committed — lifting the guard is M1-T45's own call |

## M2 — Booleans

| ID | Task | Status | Notes |
|---|---|---|---|
| M2-T1 | `bool_clause`, `array_bool_or`, `array_bool_and` | DONE | — 9 models, 117 new unit checks, all six builtins. `bool_clause` is DOMAIN-consistent, justified by `Explanation.Clause` with **no ADT change** — D-0019 arriving from the Boolean side. **13 deliberate breaks watched go red**, and break 7 found D-0032 |
| M2-T2 | `bool2int`, `bool_eq`, `bool_not` channelling | DONE | — `bool2int` BOUNDS, all four pushes tested individually. Breaks 5 and 6 (either direction deleted) are **invisible to the model suite** — the search recovers the same answer and the output is byte-identical. The silent weakness its header predicted, now measured |
| M1-T66 | Nothing but a string match holds up M1-T55's bridge | **STILL OPEN, and now sharply bounded** — half done 2026-09-18 (agent-bridge, merged). **Done**: the bridge's absence is observable *at its derivation* rather than in proof text. `Search.bridges` leaves a `Search.bridge` record per settle (the decision, the literal assumed, the bound settled onto, the holes crossed, and the id+level of the line stating each hole), plus one `Trace.record_citation` per named hole; `bridge_derivation` and `bridge_across_levels` in `test_matrix.ml` read them. Watched red with `bridges` made a no-op: *"the scene derived 0 bridge(s), expected 1 … this is NOT a formatting change"*. **It also closed a real gap rather than only instrumenting**: `Trace.emit` skips level-start entries, so a **decision** settle recorded no I-S4 edge at all — and that is the one settle written at the *deepest open level*. **NOT done, and now known to be unreachable in M1**: a *checker-level* observation. With `bridges` disabled the proof is still ACCEPTED, and structurally so — the checker reaches the settled bound either by the hole's own trace line (the clause M1-T56 gives every interior hole, which `Trace.emit` puts on the page **before** `bridges` runs) or, failing that, by `int_lin_ne`'s big-M `.opb` rows, which no `w` can retire (I-X10); `Ne` is M1's only hole-puncher. Measured, not argued: `br_unnamed` was empty in both scenes, so the first route was always available. **So the remaining half is M2-L3's to inherit**, not M1's to finish: a learned clause citing across levels is the case I-S4's level argument does not cover, and `bridge_across_levels` already exhibits a bridge written deeper than the decision it bridges, citing a level-0 hole line. Do **not** delete the bridge on the strength of this row | **Found by M1-T45 and independently verified by the orchestrator on 2026-09-17.** With `Search.bridges` disabled outright, the suite stays green: **34/34 models pass and exactly ONE unit check reddens** — M1-T55's own byte-level assertion that the `rup +1 hx_ge_2 +1 ~hx_ge_1 >= 1` line is in the proof. No checker rejects a hole split with or without the bridge. Same when the nogood negates the settled bound instead of the assumed literal. **This is I-X10's phenomenon, not a new one**: the proof rests on the encoding's own rows, because every hole M1 can punch is punched by a disequality whose `.opb` rows unit-propagate the exclusion given bounds already on the page. So the bridge is *correct and belt-and-braces*, and its necessity is unobservable — which is fine until it is not. **M2-T3 is when it stops being fine**: a learned clause citing across levels is exactly the case I-S4's level argument does not cover, and the bridge is what would make the settle step citable. Do not delete the bridge on the strength of this row; the task is to make its absence observable by something other than a text pin |
| M2-T13 | `test_random.ml`'s coverage assertions are seed-dependent, so they are flaky by construction | DONE | **DONE 2026-09-18, agent-flaky — and it was five assertions, not the one this row named.** All five `!x > 0` coverage checks had the problem (deep decisions, root conflict, div>1, div-remainder, ne-moved-bound). Route chosen: **bounded generate-until-reached** on the *same seeded stream* — the per-case logic was extracted into `process_case` so a top-up phase (cap 4000 extra draws) can keep drawing until every shape appears, which keeps a run reproducible from one `BAGUETTE_RANDOM_SEED` rather than trading one flake for an unreproducible suite. The half that mattered: the five now route through a new `coverage_check`, distinct from `check`, whose failure prints `COVERAGE-GAP (generator problem, NOT a proof rejection, NOT a soundness failure)` instead of `FAIL` — so a coverage gap can no longer be misread as a rejection in the one file where a red is supposed to be believed. **Evidence**: 130-seed sweep, 0 failures, and the top-up demonstrably *engaged* on seeds 21/53/68/89 rather than being a no-op. The agent hedged that these were only *likely* analogues of the original four; **the orchestrator then measured it and the hedge comes off.** Built the pre-fix binary from `62962b8` in a separate worktree (md5-distinct from the fixed one, per CLAUDE.md's rule that a comparison sharing a binary is no evidence at all) and ran both: seeds **21/53/68/89 FAIL on the old binary and pass on the new**, with top-up draws 9/5/27/14 exactly matching the reported numbers, and control seed **3 passes on both**. The old failure printed `FAIL the generator reaches a disequality pruning that moves a bound` — bare `FAIL`, indistinguishable from a soundness red, which is precisely the defect. No genuine proof rejection found. Gate green: 1625 unit checks, 252 matrix, 44 mutation, 34/34 determinism. *Original finding, kept*: the orchestrator's 120-seed sweep on 2026-09-17 measured `!ne_bound > 0` failing on 4 of 120 seeds (~3%), green on the default seed only. |
| M2-T14 | **Format-2.0 vacuity sweep**: find every test lane that passes for a reason other than the one it claims | **DONE 2026-09-18** (agent-fmt2). **It was not the only one — three more, all the same defect**: two artefacts disagreeing about format, the checker refusing the file on the *grammar*, and a lane asserting REJECTION taking that refusal as evidence. Worst was `test_mutation.ml`'s `rows_that_refute_alone` — **D-0030's own certification that an instance is not hollow, itself hollow under 2.0**: it wrote the `.opb` at the env format but the proof as literal 3.0 text citing `@c<i>`, so every row was refused on the grammar and it reported "no row refutes alone" for *every* instance. Only its own control made it visible. `test_proof.ml`'s 3.0-only blocks resolved their checker via `Checker.find` (which honours `$VERIPB`) and handed a labelled `.opb` to 2.2.2: 18 ACCEPTED lanes red, REJECTED lanes vacuously green. 20 blanked-trace controls in `test_trace.ml`/`test_matrix.ml` asserted off the exit code alone — latent, now assert *which* rejection. **Nothing asserts less**; test (c)'s label-based check is now resolved by **content**, checked in both formats, with the 3.0 label check kept to cross-validate. **No soundness problem: nothing accepted a proof that should have been rejected.** Verified by the orchestrator under both configurations: `test_proof` 18 FAIL → 0, `test_justify` 1 → 0, `test_mutation` 3 → 2 (deliberate, see **M2-T15**) | M2-L5 found, unprompted, that `test_justify.ml`'s M2-L0 break lane **passed on a parse error**: `conclusion_break_opb` wrote a 3.0-labelled `.opb`, so under `BAGUETTE_PROOF_FORMAT=2.0` the Python 2.2.2 checker died at `:3:1: Expected number` and the lane asserting REJECTED "passed" without the checker ever judging the derivation. Fixed (`92e03a1`), measured 5 FAIL → 1 in that configuration. **The question this row exists to answer is whether it was the only one.** The gate runs under 3.0 only, so *nothing* routinely exercises the 2.0 path, and D-0020/D-0030 already state the rule being violated: a lane rejected without the checker judging an inference is not a pass. **Tests**: (a) run every suite under `BAGUETTE_PROOF_FORMAT=2.0` **and** `VERIPB=~/.local/bin/veripb`, which is the configuration that exposed it — the default `Checker.find` prefers 3.0.2 and so never reaches the Python checker at all; (b) for each break lane that asserts a REJECTION, assert **what the rejection says**, not merely that one happened — a wording assertion is what distinguishes a judgement from a parse error, and it is what caught this one; (c) fix `test_justify.ml:693-700`, which asserts `false` under 2.0 because its check reads a **label** and 2.0 has none — make the check by **content** instead, so a 2.0 run is not permanently red by one check (a permanently-red run trains people to ignore red); (d) every `~labels` / `write_opb` call site audited against `Encoding.write_opb_for`, since that is the shape of the defect |
| M2-T16 | **Remove proof format 2.0 from the project entirely** (D-0046) | **DONE 2026-09-18** (agent-drop). `grep` for `BAGUETTE_PROOF_FORMAT`, `V2_0`, `2.2.2` and `?labels` is empty outside `WORKLOG.md`, `docs/DECISIONS.md` and `docs/PROOF-FORMAT.md` §2, all of which are history and marked as such. **(b): artefact bytes are byte-identical across all 38 models** — `.opb`, `.pbp` and stdout — with the binary hashed on both sides and different (`8223711a` → `0011dc99`), which is what "labels were already on under the default" predicted. **(c): 38 hunks across 16 files** carried dual-checker prose or a dual-wording matcher and every one was simplified deliberately; **10 of them were executable matchers** that tested two strings and now test one (`test_proof`'s `says`/`deletion_wordings`/`checker_for`/`veripb_v3`, `test_trace`'s and `test_matrix`'s `rup_rejection_wordings`, `test_learn`'s `rejection_wordings`, `test_justify`'s `wordings` and `reduce_break_checkers`, `test_random`'s `not_contradicting`), the rest comments. Verified by grep: no surviving `M1-T46` / "share no substring" / "both checkers" text outside `WORKLOG.md`, `DECISIONS.md` and this row. **(d)** `Checker.find` and `scripts/checker.sh` rewritten for one checker and now say outright that there is no second implementation; a missing checker is still a FAILURE. **(e)** D-0023/24/25/30 carry a HISTORICAL marker saying which half is still in force; `PROOF-FORMAT.md` §2 is retitled historical with the still-live parts (the `pol` operators, the `pol`-over-`rup` rule, the traps) moved into §2a where they belong. **Unit checks 1986 → 1972**, and the 14 are accounted for one by one: 10 were the second leg of a format matrix (`test_learned_survives_the_backjump` ×4, `test_reduction_truncation_is_rejected` ×6), 4 were lanes whose 3.0 twin asserts strictly more (`test_writer_rules`, `test_veripb_accepts`, one of `test_writer_levels`' three, and the 2.2.2 one-way-door probe). Matrix 280, mutation 44, models 38/38, determinism green, peak RSS 10.6 MB | Measured surface: `lib/proof/writer.ml` (42 hits, the core — `V2_0` and every `v3 t` branch), `test/unit/test_proof.ml` (43, the format matrix), `docs/PROOF-FORMAT.md` (38, §2 is the 2.0 contract), then small footprints in `opb.ml`, `encoding.ml`, `checker.ml`, `scripts/checker.sh`, `scripts/mutate_proof.sh`, `docs/SPEC.md` §4.1, `bench/`, and ~10 test files. **This lands atomically or not at all**: removing `Writer.V2_0` breaks every test that names it, so the code half and the test half cannot be separate sessions. **Tests**: (a) the gate green with no `BAGUETTE_PROOF_FORMAT` anywhere — `grep -r` for it, `V2_0`, `2.2.2` and `?labels` must come back empty outside history; (b) every model's proof still verified by 3.0.2, artefact bytes compared before and after and any diff **explained**, since labels become unconditional rather than conditional and that should change *nothing* (they were already on under the default); (c) **M1-T46's sites simplified deliberately**, not left carrying comments about a checker that no longer exists — a stale "match both wordings" comment is worse than none; (d) `Checker.find`'s not-found message and `scripts/checker.sh`'s resolution order rewritten for one checker, keeping the property that a **missing** checker is a FAILURE and never a skip; (e) history preserved — D-0023/24/25/30 and PROOF-FORMAT's account of what 2.0 did stay, marked historical, per D-0046 **Independently verified by the orchestrator**: token sweep clean (the four surviving code mentions are sentences saying the thing was removed); gate green at **1972 ok / 0 FAIL**, 280 matrix, 44 mutation, 38/38 models, determinism, width lint and fmt; and the **missing-checker-is-a-FAILURE** property intact in both `Checker.find` and `scripts/checker.sh`, which is the one thing a single-checker rewrite could quietly have dropped. One stale comment survived the grep because it was *rationale*, not a matcher — `test_justify.ml` still called a 2.0 run "permanently one check red" — fixed separately. |
| M2-T15 | **Should a mutation lane's `expect` be format-dependent?** Raised by M2-T14, not decided | **CLOSED 2026-09-18 — dissolved by D-0046.** The question does not exist once there is one format. Its two deliberately-red lanes go with it. The part that must survive the removal is the *behaviour* the lanes exhibited: the harness reported a mismatch between a lane's registered `expect` and what the checker actually did, as a **failure**, and refused to re-register itself — *"the lane has found something: say what, do not re-register it to match"*. **Keep that.** | Under **3.0** a constraint id is a **label**, so dropping the line that defines it is always a parse error whichever line you drop. Under **2.0** ids are **positional**, so dropping a line **renumbers** everything after it — the citations still resolve, just to different constraints, and the checker reads a well-formed proof and **judges an inference**. Measured on `triple_unsat` and `lin_unsat`, both dropping line 5 (a `pol`): under 2.0 veripb answers *rejected-on-the-derivation* where the lane is registered `Unevaluated`. See **D-0030's 2026-09-18 amendment**. The harness reports the mismatch as a **failure** and refuses to re-register itself — *"the lane has found something: say what, do not re-register it to match"* — and **that behaviour is correct and must be kept**. The question is only whether `expect` should be per-format. Against: it doubles the registration surface and makes a lane's meaning depend on an environment variable, which is exactly how M2-T14's bug class arose. For: it is honest about what each checker actually does. **Until decided, a 2.0 run of `test_mutation` has two expected failures and they are findings, not regressions** |
| M2-T3 | Clause learning from conflicts (1UIP), with proof steps | **SUPERSEDED — split into M2-L0 … M2-L9 below** | Still the big one; the split is **D-0044**, which fixes the learned-constraint type as a **PB inequality of which a clause is the degree-1 case**, so that staging clauses first is not a commitment to them. The `## Starting M2-T3` briefing in `WORKLOG.md` is **still current** — its preconditions (M2-T7/T8/T9), its three things-that-will-bite (I-S4, M1-T66, I-X10/D-0040) and its baseline numbers all carry over unchanged to M2-L0 … M2-L9. Read it first |
| M2-T4 | Learned-clause deletion + matching proof `del` | **ABSORBED by M2-L4** | Unchanged in substance; it moves into the learning sequence because deletion policy and the `del` pairing are the same task whether the learned object is a clause or a PB row. M1-T29's pair spelling is what it uses |
| M2-T0 | **Amend D-0011**: D-0015's `Model_row` closed its ADT gap, so multi-row derivations are permitted | DONE | A literal reading of D-0011 forbids writing a Hall-interval justification at all, and GCS's cites one line per Hall value *and* per Hall variable. Decision record only, no code. **Must land before M4-T1** — **D-0027.** Restated as a rule about *naming* rows, not counting them: citation is unrestricted, so M4-T1's Hall justification is writable with no further decision. The decomposition default stands and `int_lin_eq` is **not** re-fused. Grounded in shipped output — `lin_unsat` already emits a one-`Combine` derivation citing two instances' rows, verified since M1-T12, so the literal reading was already false of working code |
| M2-T5 | `Domain.result` reports change granularity; engine masks wakes by trigger kind | **DONE** | Delivers what `ARCHITECTURE.md` §2 already promises (`NoChange \| Bound \| Holes`) and the code never did. Ship a `BAGUETTE_DEBUG` I-P2 re-run check in the same round — I-P2 currently has no test, and a skipped wake is invisible except in the node count — `Domain.change` (`NoChange | Bound | Holes`) + engine trigger masking + a `BAGUETTE_DEBUG` I-P2 re-run check. **The headline is the measurement, not the code: with the mask deliberately made unsound, all 29 models pass** — orchestrator reproduced this (29 passed, 0 failed; only the new unit scenes go red, 4 FAIL). The mask never fires on the suite at all (`masked = 0` on every model, 970 propagator runs, 1210 wakes), because no `Domain.Holes` change occurs anywhere: `int_ne` is the only value-remover and `settle` turns its removals into bound moves. All evidence is in the unit scenes, and that is stated rather than let look tested. Trigger derivation settled by **D-0034** |
| M2-T6 | Do not re-wake a propagator from its own prunings; aliased-scope veto + claim re-checker | TODO | `linear.ml:22-29` claims single-pass idempotence; `engine.ml:97-98` re-enqueues it anyway. Copy GCS's two guards, not its claim/replay engine. Adopt the re-checker in the *same commit* (GCS #889: 767 → 1,089,375 nodes). Depends on M2-T5; re-run the proof suite, not just unit tests |
| M2-T7 | Record the propagator instance id on every trail entry and on `Conflict` | **DONE** | Closed 2026-09-17, so **M2-T8 is unblocked** and D-0011's named blocker ("should be closed before M2-T3 starts, not during it") is honoured. The engine brackets each `run` with `Store.with_running inst.id` and `Store.apply` stamps `entry.prop`; **no mutator takes an id**, so a propagator has nothing to get wrong. The rejected alternative — each propagator passing its own id — is unenforceable, because there the caller *is* the authority and there is no second source of truth to check. `conflict_facts`' one-shot slot is **gone**, the prize this row named: `Conflict of Explanation.t` became `Conflict of Store.conflict` and the D-0018 point 3 facts travel in the returned value, with `no_facts () = []` reproducing the un-armed slot exactly — which is why **all 102 proof artefacts are byte-identical**. `Engine.check_attribution` reads the stamp back **always on, not `BAGUETTE_DEBUG`-gated**, because M2-T3 resolves an entry's reason constraint through this field and an id threaded but never read is decoration. Landed **I-T4**. For M2-T8: `Store.conflict` is now the single construction point for a conflict, so D-0026's swap for a declarative `Reason.t` is a field change rather than a protocol rework | Closes D-0011's own named blocker ("the trail records no propagator identity… should be closed before M2-T3 starts, not during it"). Lets `Store.conflict_facts`' one-shot slot go. **Blocks M2-T3** |
| M2-T8 | Propagator interface v2: declarative `Reason` + separate `Justification` (D-0026) | **DONE** | Closed 2026-09-17. **M2-T9 and M2-T3 are unblocked, and M3-T4 and M4 have their precondition.** `Reason.t` is a list of frozen facts and `Reason.lits` **takes no store**, so I-X6 on the reason half is discharged by the *type* rather than by discipline; `lit_of_fact` is the single materialisation point and absorbed the drop-at-declared-bound rule three propagators each carried a copy of. **No new `Explanation` constructor was needed.** I-P4 and I-P5 are genuinely one obligation: every mutator takes one `Reason.justified`, `no_facts` is deleted, and a caller with no facts writes `Reason.none` and is seen doing it. `linear.ml`'s per-pruning trail scan is gone — O(1) per term from `sup_lo`/`sup_hi` arrays `apply` maintains and `undo_to` restores — with the honest caveat that a settle past holes still costs one bounded scan per hole. **All 102 artefacts byte-identical**, re-verified with both binaries hashed. Ten breaks performed; the four that matter are recorded in the handoff. The agreement check **fired on correct code twice** before it was right, and ships saying what it still cannot catch. Left undone deliberately: D-0026's "`Explanation.t` loses the reason-shaped constructors" — `Clause` and `Decision` are consumed as *derivation* forms and deleting them is a separate decision |
| M2-T9 | Literal → defining-proof-line index in `Justify.ctx`, populated by `Trace` | **DONE** | Closed 2026-09-17. `Justify.ctx.stated` keys a sorted literal **set** structurally over `Lit.t` rather than over rendered OPB names, **because `Lit.sanitize` is non-injective** — keying on names would have been a silent aliasing bug. Reuse requires the indexed line's level ≤ the current level, which is **I-S4 as a precondition** and is what keeps `Search`'s nogood out of reach. Lookup only, never iterated, so no emitted byte depends on hash order. **D-0019's clause-in-a-`pol` gap is NOT closed, verified not assumed**: the duplicate and the trace line had byte-identical bodies, so the `pol` derives what it always did, and 3.0.2 still gives M1-T44's wording — the gap is about *currency*, not identity. **Break A is the one to remember**: pointing the index one id high reddens 6 checks, leaves 34/34 models green, and veripb **accepts a `pol` citing the wrong line**; only the check that reads the reused id's own line back out of the proof text caught it. Its structural limit — a `pol`'s content is computed by the checker and unknown to us — is what produced **D-0043** | Closes D-0009's "not today" by adding the field it names as missing; `Justify` already has the level-tagged memo and lockstep wipe. May also close D-0019's clause-in-a-`pol` gap — **verify, do not assume**. Depends on M2-T8 |
| M2-T10 | Harness: per-node consistency checking against the brute-force oracle | TODO | Nothing verifies the `consistency` tag that SPEC 2.2 makes normative; today's oracle checks soundness only. Must assert **at least** the declared level — `ne.ml` deliberately declares the weaker `Value`, and weakening a test to match is forbidden. Needs a search trace hook |
| M2-T11 | Fuzzer: randomise branching order from the announced seed | DONE | One seed already drives model generation; drive variable/value choice from it too. M1's whole nogood story is that the branch's own trace supports the refutation, so a different tree is a different trace. Depends on M1-T16 — **60/60 artefacts byte-identical, hashes shown**, with a control proving the check can go red (flipping the branch side moved 19 artefacts and broke 6 models). ~500k solver runs swept; 158 seeds green, **2 red**. Found a CLI-reachable rejected proof that predates the task (M1-T44). Also fixed a guard `random_order` shipped disabled by `if true || ...` |
| M2-T12 | The model-suite artefact baseline is prose in a roadmap row, not a committed check | **DONE (determinism half); seeded-branching half still open** | **The determinism half landed 2026-09-17 as `scripts/check_determinism.sh`, in the gate** via a `determinism` target that depends on `build` (because `dune runtest` does not build `bin/main.exe`). It solves all 34 models twice and requires `.opb`, `.pbp` and stdout to agree byte for byte. **The central design choice is that it stores NO golden digest**: a committed hash would be wrong on the next legitimate proof change — M1-T29 moved 14 of 34 `.pbp` files, correctly, the same day — and a check that must be re-blessed whenever it fires trains people to re-bless it without looking. Comparing run N against run N+1 is self-relative, true of every correct version, and needs no maintenance. **agent-lazy2's warning travels with it in the script header**: byte-identity is evidence of no change, never of correctness, so this catches nondeterminism (hash iteration order, an address reaching output, an uninitialised read, a timestamp) and nothing else. Two breaks performed: the `--self-test` lane proves it catches a solver that appends a changing `.opb` comment *and* accepts the real one, and the real check run against a solver that perturbs the `.pbp` fails **34 of 34** with a named reason. Cost 0.7 s. **Still open**: that seeded branching leaves the fixed suite's solutions valid and proofs accepted. That needs `random_order`, which no CLI flag reaches (`bin/main.ml` has no seed option) and which `test_random.ml` exercises only over *generated* models — so it has to live in a test binary, and it waits for `lib/core` to be free of M2-T8. Note it must NOT assert byte-identity: a different tree is a different trace, so the artefacts legitimately differ and the property is validity, not equality |
| M2-T17 | **Explanation size as a tracked number, per model** | **DONE 2026-09-18** (agent-size). Two metrics, reported separately because they move independently: **`rup_lits`** (literals per derived `rup` constraint) and **`pol_prems`** (premises per `pol` chain). Both read straight off the emitted `.pbp`, so **no `lib/` change and no new solver counter** — which is what let this run beside the M2L sequence. Baseline over the 38 models is `bench/README.md` **§3d**; `bench/explanation_size.sh -c` is the control and is deliberately **not** in `make check`. Ranges: `rup_lits` 0.0–2.36 (26/38 models have a `rup` line; the 0.0 is `ne_self_unsat`'s legitimate empty-contradiction final line, not a defect), `pol_prems` 2.0–1999.0 (12/38), where 1999.0 and 19.0 are the two known D-0028 width fixtures and 5.0 (`lin_unsat`) is the largest non-width figure. **No non-width model looks verbose today**, which is the expected answer while every propagator is linear and the derivation is forced — the metric's job starts at M4-T1. **The control is the half worth reading.** As first delivered it passed, and the session honestly recorded that one of its three deliberate breaks — counting only `@c`-prefixed `pol` premises — was **not** caught by its fixture. That mattered more than it looked: `width_root_unsat`'s `pol` line is essentially all bare literals, so `pol_prems=1999`, the **largest figure in the entire baseline**, is produced by exactly the token class the control did not protect, and the suite-wide run only *reports* and never asserts, so the regression would have been silent. Sent back; the fixture now carries bare-literal premises with the `@c`-id count **pinned at 4 on both sides**, so the growth is attributable to the bare-literal part alone. **Verified independently by the orchestrator**, not read off the report: honest script exits 0; flattening the variant fixture exits 1; break (3) re-applied by hand exits 1 on `pol_prems did not grow (4.0000 -> 4.0000)`. Gate unmoved at 1972 ok / 0 FAIL, 280 matrix, 44 mutation, 38/38, peak RSS 9.6 MB | **No row in this roadmap asserts anything about explanation *quality*.** M2-T10 is the strength-side sibling and covers propagators; nothing covers the explanations themselves. The literature's whole quality metric is **generality** — a shorter, weaker-premised explanation prunes more later, which is why Schutt's window beats naming every task and why Downing compares three `alldifferent` propagators. The gap, stated sharply: **a sound but maximally weak explanation that names every variable in scope passes every test in this repo today, and VeriPB accepts it.** That is harmless while every propagator is linear and the derivation is forced; it stops being harmless at M4-T1, where the *choice* of Hall set is the whole game. **Scoped deliberately to `bench/` and to artefacts that already exist**, so it needs no `lib/` change and can run beside the M2L sequence: the literal counts are readable straight off the emitted `.pbp`. **Tests**: (a) a per-model size figure over the 38-model suite, committed as a baseline the way `bench/README.md` already does for bytes and seconds; (b) the figure moves when an explanation is deliberately widened — **break it and watch it move**, since a metric never seen to respond is not a metric; (c) reported per model rather than summarised, for M2-L8's reason — the point is watching them move independently; (d) **measurement only, never a commit gate**, per the `make bench` rule. |

## M2L — conflict learning: CP search, PB proof, PB learned constraints

The architecture is **D-0044**: the learned-constraint type is a PB inequality
`sum a_i l_i >= b`, and a clause is its degree-1, unit-coefficient case rather than a
separate type. M2-L3 emits only the clause case; the type does not know that. This is what
makes "start with clauses" a staging decision instead of a commitment — see D-0044 for why
that is true of *this* solver's encoding (D-0028) and not of CP solvers generally.

**Read `WORKLOG.md`'s `## Starting M2-T3` section before any row here.** It is current: the
preconditions it lists (M2-T7 `entry.prop`/I-T4, M2-T8 `Reason.justified`, M2-T9
`ctx.stated`) are closed, and the three things it says will bite — **I-S4**, **M1-T66**,
**I-X10/D-0040** — are the three this sequence is most likely to break. Its baseline is the
number to beat: 1625 unit checks, 252 matrix checks, 34/34 models, 102/102 artefacts
byte-identical, peak RSS 55 MB.

**Every row states its own test obligation, and every one of them includes a break.** A
test that has never been seen to go red is not evidence — M1-T42, M1-T51 and M2-T9's
"Break A" each found that a checker accepts a well-formed but wrong derivation, so on this
sequence *a passing proof is not by itself a result*. Where a row says "break:", perform it
and report what reddened.

`lib/core` should be held **exclusively** for this sequence. M2-T6 and M2-T10 want the same
files and queue behind, not beside.

| ID | Task | Status | Notes |
|---|---|---|---|
| M2-L0 | **Implement D-0043**: the conclusion as a `Reason.fact` on `justified`; first caller for `Writer.pol_concluding` / `Writer.implied` | **DONE 2026-09-18** (agent-concl, merged `a229509`). Required `~concludes` labelled argument feeding a required field carrying an optional value. All three tests done; (a)'s gap measured in both polarities. **Finding**: `Domain.set_lo` settles over holes (I-D2), so the derived bound and the trail bound are two different honest numbers — `conclusion_holds` admits exactly that window. **`Justify.emit_concluding` has no live caller**: wiring `search.ml`'s emission to it would move every proof artefact, deliberately left as its own decision | D-0043 is DECIDED and unimplemented, and says explicitly that it is **not** a precondition of M2-T3 and must not be folded into it. It is kept as its own row for exactly that reason, and it must be **DONE before M2-L5**, because a `pol` states a derivation the checker recomputes: a learned PB row whose `pol` derives something strictly weaker than the row we then propagate with is the one soundness gap this whole sequence introduces, and stating the conclusion is what closes it. It is *not* needed for M2-L3, whose learned clauses are `rup`. **Tests**: (a) M1-T51's measured break — a `pol` truncated to derive something strictly weaker is ACCEPTED bare and must be REJECTED once the conclusion is stated; match **both** checker wordings, never one (M1-T46); (b) M2-T8's agreement check becomes exact rather than "…or the fact has a support" — break by recording a conclusion one unit off the trail bound (I-X9's shape, the M1-T44 defect) and assert it reddens; (c) the optional partition — a decision (D-0037) and a conflict carry no conclusion, a pruning does; assert it tracks `Trace.claims`' existing line-or-no-line split, and break by giving a decision one |
| M2-L1 | `Learned.t`: the PB learned-constraint type, its runtime propagator instance, and its proof-side introduction and deletion | **DONE 2026-09-18** (agent-learned). D-0044 **converted** (27 scenes, negative control); the bet held — the instance is `Linear`, no new propagator family. D-0045's prediction **measured before fixing**, in both formats, through both checkers; the fix is `Writer.with_level`, a bracket that moves the level *in the proof*, because under 2.0 `t.tags` is not maintained so our own table never reaches the checker. **Two findings**: the I-X2 audit **cannot see a double delete** (a second `forget` is a no-op) — M2-L4 must not rely on it; and test (f)'s deliberate red line **correctly never came**, because the closure gate scans `lib/core/prop/` and this row's own bet means no module lands there | **The row that settles D-0044's central claim.** `sum a_i l_i >= b` over `Lit.t`; instantiate it at runtime as a `Linear` instance registered with the engine, since a learned row is exactly what `Linear` already propagates — no new propagator family if this holds. **Read D-0045's addendum before designing the proof-side half.** `Writer.fresh` tags every id with the writer's *current* level and has no override, and `wipe_level` deletes everything tagged at or above the retired level — so a learned constraint is deleted by the backjump that follows learning it, automatically. This row is where that is cheapest to fix, and the fix is most likely a new `lib/proof/writer.ml` entry point (an id allocated at a chosen level) rather than a call-ordering trick in `lib/core`. It must hold under **both** proof formats: 2.0 does not maintain `t.tags` at all. No conflict analysis in this row: the constraints are hand-built by the tests. **Tests**: (a) oracle round-trip — a `Learned.t` built from a model row propagates identically to that row compiled from `.fzn`, brute-forced on small domains per I-P1; (b) **the D-0044 check** — a degree-1 unit-coefficient `Learned.t` over order literals propagates exactly as `Bool_clause` does on the same scene; **if this fails, D-0044's central claim is wrong and M2-L3's staging must be revisited**; (c) I-T4 — the runtime instance gets an id, `Engine.check_attribution` accepts its prunings, and a mis-stamped id reddens (M2-T7 measured `current_prop + 1` at 121 checks); (d) I-X2 — under `BAGUETTE_PROOF_AUDIT=1` the learned id is deleted exactly once; break: drop the `del` and the audit must fail, which is M1-T29's preserved break `474a05f`; (e) **determinism** — the learned database must not be iterated in hash order; `scripts/check_determinism.sh` requires two runs to be byte-identical, and the `WORKLOG` briefing flags this as the specific way learning breaks the gate; (f) I-X10 — a learned constraint is a new pruner resting on **several** model rows, so `test_trace.ml`'s closure gate fires until it is classified `Needs_derivation`; that red line is deliberate and is how this row learns it owes the explicit `pol` (D-0040) |
| M2-L2 | Conflict-analysis scaffolding: the implication graph and the 1UIP cut, as data | **DONE 2026-09-18** (agent-cut, merged `a229509`). Criterion is a swappable record carrying its own postcondition — `one_uip`, `conflict_side`, `decision_cut` — so the 1UIP assertion is scoped to the clause path and M2-L6's slack criterion will not trip it. **Two things M2-L3 must read**: (1) `Store.lo_support`/`hi_support` is **not always the right edge** — a reason's facts are frozen at older values, so if a bound moved again the O(1) array names a *later* entry; `support_of ~before` falls back to a downward scan, and `o1_supports`/`scanned_supports` expose the split. (2) The second `entry.prop` break **reddened nothing** — stamping one high can land on another instance that *does* watch the variable, the same blind spot `Engine.check_attribution` has | Walk back from `Store.conflict` through `entry.prop`, `Store.lo_support` / `hi_support` (O(1), M2-T8) and `Reason.owners`. Produces the cut; learns nothing, backjumps nowhere. Split from M2-L3 so that the cut can be tested against an oracle before anything depends on it. **Tests**: (a) oracle — every literal in the cut is on the trail, and the model plus the cut's negation is UNSAT by brute force; (b) the stopping criterion, **as a named component, not a constant** — for the clause path the 1UIP rule (exactly one literal from the conflict level) is asserted, not assumed; the assertion is **scoped to that criterion** and must not be written as a general invariant of the cut, because it is false for the PB path — see M2-L6 and D-0044; (c) break: stamp `entry.prop` one high and assert the oracle catches it, which proves the cut actually reads the field rather than re-deriving from the trail order; (d) **holes** — an `int_ne` removal claims a *clause* (`Encoding.ne_clause_lits`, M1-T56), not a literal, so it is **non-asserting** and cannot be a 1UIP graph node in the usual way; the documented rule is to fold its reason into the bound move that consumed it, and a scene with a hole in the graph must assert that rule fired rather than the hole being silently dropped |
| M2-L3 | 1UIP **clause** learning over order literals, with backjumping and its `rup` derivation | **DONE 2026-09-18** (agent-learn3). **D-0044 fork (ii), proof-only**, chosen on a measurement rather than a preference. All six tests done; I-S4 discharged and watched fail. **The finding the row did not anticipate**: the backjump cannot rest on the 1UIP cut at all — the levels a 1UIP cut names are **not a dependency set** (a non-root node at level j rests on facts at levels the cut never names), so skipping on `Analysis.backjump_level` would skip a subtree nothing refuted. `Analysis.analyse` gained `?scope` and a `decision_closure` criterion whose level set is sound. Artefacts: **0 `.opb` changed, 0 node counts moved** on the 34 pre-existing models; 17 `.pbp` longer, +297 lines, at +4 per learned clause. **M1-T66 re-measured with learning on and the answer is still NO** — see that row | The clause case of `Learned.t`. Emit the learned clause's derivation **while the trace lines supporting it are still live**, and attach the clause at the backjump level, not the level being retired. **Those two clauses are in direct tension and D-0045's addendum shows why**: emitting while the supports are live means emitting before the wipe, but `Writer.fresh` tags the new id at the *current* level, so emitting there is exactly what gets it deleted. Do not discover this from a rejected proof — check whether M2-L1 gave you the entry point that resolves it, and if it did not, say so rather than working around it locally. Apply **semantic minimisation over order literals** before emitting: the ladder makes `x >= 5` entail `x >= 4`, so a cut may carry several thresholds for one variable in one direction and only the strongest is needed. This is cheap, strictly strengthens, has no analogue in CNF, and M2-L6 reuses it. **Read D-0044's 2026-09-18 amendment before designing the cut**: `Learned.to_linear_row` returns **`None`** on a threshold strictly inside an integer variable's ladder, which is *exactly* what a 1UIP cut over integer variables produces — so either restrict the cut to the shapes that do convert, or accept a **proof-only** learned constraint until a propagator exists. Sound either way; not optional to decide. **Tests**: (a) 34/34 models, every proof verified, artefact baseline re-measured and the diff explained rather than accepted; (a2) minimisation is idempotent and never drops a literal the cut needs — break by keeping the *weakest* threshold per variable instead and assert a model's proof is rejected; (b) **I-S4 ordering** — a test that the derivation precedes the `w` retiring its level; break by moving it after and assert the checker REJECTS, both wordings; this is the invariant the `WORKLOG` briefing names as the one this task is most likely to violate, and the argument in I-S4 explicitly does not cover a clause citing across levels; (c) node count strictly decreases on designated UNSAT models, asserted numerically against `--stats`' real node counter (M1-T36), not against the `lvl` proxy; (d) I-S3 — level on return equals level on entry, with learning on; (e) **M1-T66's own definition of done** — with `Search.bridges` disabled, something other than M1-T55's text pin must now redden, because a learned clause citing across levels is exactly the case the bridge exists for; if nothing does, say so plainly and leave M1-T66 open rather than claiming it; (f) determinism green |
| M2-L4 | Learned-constraint deletion, the matching `del`, and a retention policy | **DONE 2026-09-18** (agent-del). **The deliverable was the ownership question, and the answer settles it structurally**: `lib/core/retention.ml` owns a learned constraint's lifetime alone, because `Writer.wipe_level` now **refuses `l <= 0`** rather than relying on `Search`'s four call sites all happening to pass a decision level. **The collision D-0045 predicted was not the one that would have happened** — D-0045 warned of two owners; the real shape was **one** owner, `Search.solve` sweeping every id ever introduced while a policy evicts mid-search, which the I-X2 audit **cannot witness** because a second `forget` is a no-op. Restoring the old sweep reddens 18/65 and the checker rejects. **The policy is `keep_all`, by measurement, not omission**: nothing propagates a learned constraint (`Learned.instance` has no caller in `lib/`), so activity is the constant zero and an activity policy is FIFO; LBD over 39 models is 72 at 1, 16 at 2, none above. Cap sweep on `width_sat_depth`, reproduced by the orchestrator: eviction **monotonically increases** `.pbp` bytes (48145 → 48658 → 48793) and `del` rules (124 → 181 → 196), all three verified. Every policy is dominated by keep-all. Recorded as **D-0051**, including what would reverse it. 65 new checks in `test/unit/test_retention.ml`; tests (a) I-X2, (b) I-X3, (c) bytes-both-ways all done with breaks — guards off reddens 10/65, old sweep 18/65 plus a checker rejection, `wipe_level 0` re-allowed 2/65. **Verified independently by the orchestrator**: 2129 ok / 0 FAIL (2064 + 65), 39/39 models, and all **117 artefacts byte-identical** at the default with genuinely different binaries. Absorbs M2-T4 | LBD-like or activity-based; the policy is this row's to choose and to justify with a measurement, not with a citation. **D-0045's finding applies here directly**: `Writer.wipe_level l` deletes every id tagged at level `>= l`, so if a learned constraint is tagged at the level it was learned at, the backjump deletes it — and if this row's policy *also* deletes it, that is the I-X2 double-delete test (a) asks about, arriving from two owners rather than one. Decide who owns a learned constraint's lifetime before writing the policy. **Tests**: (a) I-X2 audit with deletion on — every learned id deleted exactly once, no double-delete when the policy and the backjump both want it gone; (b) I-X3 — a deleted reason is not referenced by any live trail entry; break: delete a constraint still cited and assert it is caught rather than discovered by the checker; (c) proof `.pbp` bytes measured with the policy on and off, because M1-T59 found duplicate `rup` lines that are harmless per-proof and stop being harmless per-pruning, which is what this sequence creates |
| M2-L5 | Reduction as a named, swappable component: `division` and `roundToOne` | **DONE 2026-09-18** (agent-reduce). `{ name; reduce : view -> outcome option; postcondition : view -> outcome -> bool }`, the shape M2-L2 set. The pluggable field is **`outcome.derive`**, mapping the explanation of the *original* row to that of the *reduced* row — so **M2-L7 plugs saturation in as another value in `reductions`** with `weakened = []`, `divisor = 1`, `derive = fun e -> Saturate e`, and the analysis never learns which rule it holds. **D-0044's table held exactly**: `Weaken lits` then `Combine (summands, divisor)` expresses both rules, so `explanation.ml` needed no constructor **and `justify.ml` needed no edit at all**. Test (c) is the checker-level control M1-T42 could not build: a truncated reduction is ACCEPTED bare, REJECTED once M2-L0's conclusion is stated, under **both** checkers, each in its own wording, with the test printing which it got. **Honest break**: the first (b) break reddened *nothing* — weakening a falsified literal in a *slack* reason leaves the pivot propagating; the break in the file uses a *tight* reason, where weakening makes the row vacuous. **For M2-L6**: call `outcome.derive`; do not rebuild the step from `weakened`/`divisor` (saturation will have neither), and `reduce.ml` reads no store, so M2-L6 owns freezing `falsified` at the propagation (I-X6) | D-0044's table: both are expressible **today** with `Weaken lits` followed by `Combine (summands, divisor)`, so this row adds no constructor to `explanation.ml`. Keep the signature pluggable — saturation (M2-L7) and, if ever, MIR must drop in without touching the analysis. **Tests**: (a) hand-built rows with the expected reduced output, per RoundingSat's definition, so the component is pinned independently of the search; (b) property — the reduced reason still propagates the same literal and still has negative slack, i.e. the conflicting invariant is preserved; (c) **the checker-level control M1-T42 could not build** — with M2-L0's conclusion stated, a truncated reduction must be REJECTED; a bare `pol` accepts anything well-formed, which is why this row is sequenced after M2-L0 and not beside it |
| M2-L6 | PB conflict analysis: eliminate the pivot by linear combination + reduction, with the M2-L3 clause path as fallback | **DONE 2026-09-18** (agent-pb) — **and its own test (a) returns an honest negative that the row must not be read past.** Where the PB path succeeds it derives the **empty contradiction**, not a non-trivial inequality: on `backjump_lineq_unsat` the two `int_lin_eq` halves give `sum>=3` and `sum<=2`, which add to `0 >= k/2` in one elimination. Strictly stronger than the clause — it entails it, and the clause path has no route to it — but **degenerate as a propagation result**, and **a scene where a non-trivial learned inequality outpropagates its clause was looked for and NOT FOUND**. ~~Measured by the orchestrator: `pb-stronger` **36 of 36** learned rows, but every one of those 36 is the degenerate case.~~ **THIS FIGURE IS WRONG — corrected 2026-09-18 by M2-L11 and independently re-measured on `main` before that row merged**, by counting emitted `ia` rows rather than reading a counter: **36 learned rows, 26 carrying terms, 10 degenerate.** The 10 are exactly the `int_lin_eq` family (`backjump_lineq_unsat` 3, `near_limit_unsat` 3, `offset_unsat` 4) — the family this row's own test (a) inspected — while `width_sat_depth` **alone** contributes 24 non-degenerate rows. The conclusion was true of what was looked at and was then stated of the suite, with **no counter to contradict it**; `pb-nondeg` (M2-L11) is that counter. D-0047 was written on the wrong figure and is amended. **Also wrong in `pb_analysis.ml`'s header, now fixed**: `Postcondition_failed` was called the dominant non-`No_row` fallback; of 50 it is **2**, against `No_pivot` **26** and `No_row` **21**. (a2) asserted on the criterion itself — a row with three conflict-level literals is accepted, one asserting nothing below the conflict level is rejected, `Analysis.one_uip` untouched. (b) **fallback rate 0.581** suite-wide, independently re-measured: **86 attempts, 36 learned, 50 fallbacks, 21/38 models**, and `attempts = learned + fallbacks` holds; 0.00 on lineq, 1.00 on bool, so a silently all-fallback build cannot pass as working. (c) 38/38 under formats 3.0 **and** 2.0. (d) `Checked.Overflow` raises on coefficient *and* degree; the loop turns the raise into a fallback. (e) the run-derived oracle is **vacuous and says so** — these models' antecedents have no 0-1 model, which is *why* the search conflicts — so the non-vacuous oracle is exhaustive over the primitives instead. `explanation.ml` untouched: **D-0044's table has now held three times**. **Koops et al. read, and it corrected two things**: merging adjacent weakening steps is *CakePB's own pass* (§5.4), not an emitter obligation, so M2-L5 needed no change; and partial weakening before a non-normalised division is §3's **MIR**, strictly stronger than both `Reduce` rules and belonging as a **third `Reduce.t`** — recorded, not taken. **Read M2-L11, which is where the gain this row was expected to deliver actually lives** | Fall back to the clause path when the combined constraint stops being conflicting — IntSat, HaifaCSP and LLG all need this fallback, which is why M2-L3 is permanent rather than transitional (D-0044). Read Koops et al. (CP 2025) §5–6 on the emission path **before** designing it: partial weakening before a non-normalised division, and merging adjacent weakening steps on `pol` lines. **Tests**: (a) a scene where the learned inequality is strictly stronger than the clause it replaces — assert it propagates where the clause does not, which is the whole justification for the row; (a2) **the stopping criterion is the PB one, and this is not a detail**: learning the first assertive constraint gives the highest backjump *in SAT* and Le Berre et al. (arXiv 2107.13085) show there is **no such guarantee for PB constraints**, so an assertive constraint is not a sufficient stop condition; assert that the criterion is the slack-based one and that a cut carrying several conflict-level literals is accepted rather than rejected — the M2-L2 clausal assertion must not fire here; (b) fallback rate instrumented, reported by `--stats`, and asserted non-degenerate on a fixture, so a silently all-fallback build cannot pass as working; (c) every model's proof verified; (d) **I-X8 / D-0029** — coefficient growth must `raise`, not wrap; a scene forcing growth past the cap asserts the raise, and a run that dies against the memory cap is a finding to report, not an obstacle to route around; (e) oracle — the learned inequality is entailed by the model, brute-forced on small domains |
| M2-L7 | `Saturate` in `Explanation.t`, and saturation-based reduction as the second plug | TODO — **needs its own decision record** | `explanation.ml`'s header forbids a new constructor without one, and that rule is not suspended by D-0044, which authorises the learned-object type and not a constructor. The proof half already exists: `Writer.Pol.saturate` emits ` s` and PROOF-FORMAT §2a documents it — only `Justify` cannot reach it. Note that division is **exponentially stronger** than saturation (D-0044's citations), so this row is for coverage of the reduction space, not for an expected win; say so in the record rather than discovering it in a benchmark. **Tests**: (a) `Justify` renders ` s` and the checker accepts it; (b) a scene where division and saturation yield different learned constraints, both verified; (c) exhaustiveness — extend `force`, `lits` and `to_string` with no catch-all case, so the warning finds every site, per the module header **Start from D-0047, not from zero.** M2-L6 returned a negative and D-0047 states it as a falsifiable prediction with M2-L11 as the experiment; whichever way M2-L11 lands changes what this row should try first, so read that record and M2-L11's result before designing the constructor. **DEMOTED 2026-09-18 by D-0050 — do not take this next.** D-0047 promoted it conditionally on M2-L11 *failing* to produce non-degenerate rows; M2-L11 **succeeded** (28 of 38 non-degenerate), so the promotion condition was never met. And saturation `cᵢ := min(cᵢ, d)` is **uniformity-preserving**, so it cannot rescue a row `to_linear_row` refuses — those are refused for *missing rungs* or *mixed coefficients*, neither of which saturation undoes. Expected movement in the `pb-*` figures: **none**. If taken for coverage anyway, scope it to the constructor plus the `Reduce.t` plug with a **pinned prediction that no suite figure moves**, asserted by a test rather than hoped for in a benchmark. **D-0054 changes what this row is for.** Saturation is a *reduction* rule and reduction is a solving-side device — it exists to keep the resolvent conflicting. Judge any future `Saturate` by whether it makes a learned row **propagate more**, not by whether the checker accepts it. Still sequenced after M2-L13. |
| M2-L8 | Benchmark: learned-constraint count, fallback rate, `.opb` bytes, `.pbp` bytes and verify seconds, reported separately | **DONE 2026-09-18** (agent-bench). Per-model, printed **beside** the byte and time columns rather than summarised, because the point is watching the five move independently; counts are summed, bytes and seconds never are. **Honest about its own limits**: 23 of 38 rows spend ≥90% of wall-clock outside the solver, so only `width_sat_depth` (5%) and `width_root_unsat` (27%) have meaningful timings, and **no count is divided by a time anywhere in the report**. **The control earned the row**: three asserted scene pairs under `bench/control/`, exiting non-zero on misclassification, watched fire against three deliberately broken classifiers (nodes-alone, hard-wired `proof-only`, hard-wired `CHANGED`). Its `ctl_samenodes` scene **found that a nodes-alone verdict had gone stale when backjumping landed** — with `nodes = 2·decisions + 1 − skipped` a scene can hold **nodes at 9 = 9** while decisions go 5→4 and skipped 2→0, a genuine CHANGED that nodes-alone reports as proof-only. The verdict now consults nodes, decisions, maxdepth **and** skipped. Verified by the orchestrator: control exits 0 and states it passes in both directions. Also corrected a figure of mine — **21** of 38 models learn a clause, not 18. `-F` and every format knob removed from `bench/` ahead of D-0046 | Extends M3-T5, which already separates proof size from verify time because GCS measured the two moving in **opposite** directions at an identical search tree. Measurement only — `make bench` is never a commit gate. **Tests**: the harness itself, plus a control proving the report can distinguish "proof-only change" from CHANGED, which is the distinction M1-T36 had to add and the one a learning benchmark needs most |
| M2-L9 | **SPEC §3.4 question: restarts.** Raise it; do not decide it unilaterally | **RAISED as D-0045** (2026-09-18) — the question is on the record with no verdict, which is what this row asked for. It stays open until M2-L8 supplies the measurement that decides it | SPEC §3.4 disables restarts. Learning without restarts recovers a fraction of its benefit, and nogood-recording-from-restarts (Lecoutre et al.) is unavailable to us for the same reason. `docs/SPEC.md` is the authority and a change to it goes through a decision record, so this row is a **question raised**, not a change made. Sequenced last deliberately: M2-L1 … M2-L6 are all sound and useful without it, and deciding it early would be deciding it without the measurement from M2-L8. **Tests**: none until it is decided; when it is, the proof must survive a restart — I-X4 says the proof is append-only and never rewound, so a restart is deletion, not truncation, and I-S3 must still hold |
| M2-L10 | **Backjump instance coverage**: models that actually exercise a skip, so M2-L4/L6/L8 have something to measure against | **DONE 2026-09-18** (agent-cover). Three models, three *different* propagator families, and the shapes asked for rather than three copies of one: `backjump_deep_unsat` skips **3** siblings across more than one level; `backjump_bool_unsat`'s two learned clauses both **convert**; `backjump_deep_unsat` and `backjump_lineq_unsat` (int_lin_eq under checked arithmetic) do **not**, so both sides of D-0044's fork are exercised. Each verified under **3.0/3.0.2 and 2.0/2.2.2** — six runs, all `s VERIFIED UNSATISFIABLE`, no rejection, no existing model or expected output touched. **Coverage re-measured by the orchestrator: models with a skip 1 → 4, total skips 2 → 9, learned 77 → 86, convertible 11 → 13.** (The agent's report said 10 skips; its own per-model table sums to 9, and 9 is what `--stats` gives.) M2-L4/L6/L8 are unblocked | Measured across all 35 models after M2-L3 landed: **18 of 35 learn a clause** (77 clauses), but **exactly ONE exercises a backjump skip** — `backjump_unsat`, the model M2-L3 itself added — for **2 skips in the whole suite**. So the *derivation* path is well covered and the *skipping* path rests on a single instance. **M2-L4**'s retention policy, **M2-L6**'s fallback rate and **M2-L8**'s benchmark would each be tuned against two skips in total, which is not a measurement. Also measured: `to_linear_row` accepts **11 of 77 (14%)**, concentrated in the `bool_*` models plus `bool_reif_unsat`, `ne_eq_unsat` and `trace_settle_sat` — so convertibility is model-dependent, which is the real argument for D-0044's fork (ii). **Tests**: (a) each new model asserts `skipped > 0` via `--stats`' real counter, not by inspection; (b) every new proof verified under **both** formats and both checkers; (c) a spread of *shapes*, not one shape repeated — at least one where the backjump crosses more than one level, and at least one where a learned clause is `convertible` and one where it is not, since those take different paths; (d) declared domains stay in the single or low double digits (D-0028), which is the constraint that makes this task fiddly rather than trivial |
| M2-L11 | **The ladder chain as a row**: give PB analysis the reason `Linear` actually uses, so a learned inequality can be non-trivial | **DONE 2026-09-18** (agent-ladder). **The lift works and is load-bearing; the premise it rested on was partly wrong.** `lib/core/ladder.ml` (new module, so the `Order_reason`/`Encoding` coupling is asserted without being disturbed). On `test/models/ladder_lift_unsat.fzn`: 2 conflicts → **2 rows learned, 0 fallbacks, both non-degenerate, 2 rungs cited**; with `pb_ladder = false`, which is behaviourally the M2-L6 build, **0 learned, 2 fallbacks**. Suite-wide it fires on **one model of 39**, converting 2 fallbacks into 2 non-degenerate rows — a small number, and D-0049 says so plainly. New counters `pb-nondeg`, `pb-lifted`, `pb-rungs` on `--stats`. All four tests done with their breaks; (c)'s break — one rung written at multiplier+1, sound and well-formed with every id live and only the arithmetic wrong — **reddened on the checker's judgement at full strength**: *"Expected constraint is not syntactically implied by the constraint at the hint."* (d) is now asserted for the first time and **`Linear` could not have asserted it** — `summands_of_snap` discards the constant, so a drift would have been silent until `Ladder` used it. Design recorded as **D-0049** (substitution rule; lift only non-falsified terms, and why that is an argument rather than a missing case; retry-not-replace; I-S4 discharged structurally). **Verified independently by the orchestrator**: 2064 ok / 0 FAIL (1972 + 92), 39/39 models, `s VERIFIED UNSATISFIABLE` on the fixture, peak RSS 37.7 MB. **Next honest question is not "lift more"**: the lifted rows are non-degenerate but do not convert (`pb-convert 0`), so it is what a learned row must look like before `Learned.to_linear_row` accepts it | **Our integer propagator is stronger than PB propagation on the same row, and that is why M2-L6's learned inequalities are degenerate.** `3a + 2b <= 14` with `lo(b) = 4` lets `Linear` deduce `a <= 2`; the row *alone* has slack 6 against pivot coefficient 3, so PB propagation on it deduces nothing. The strength is not in the row — it is in the **order-encoding ladder implications, which live in separate `.opb` rows**. So the true reason for a `Linear` pruning is *model row + a ladder chain*, which `Linear` already builds as an `Explanation` (`Order_reason.weaken_declared`, D-0010) but **never as a row**, and the PB analysis therefore never sees it. Until it does, eliminating a pivot can only reach the contradiction, never a useful intermediate inequality — which is exactly what M2-L6 measured. **Tests**: (a) the `3a+2b<=14` scene above, asserting the combined row propagates `a <= 2` where the bare model row does not; (b) `pb-stronger` counts a **non-degenerate** learned row, i.e. one that is not the empty contradiction — M2-L6 leaves that counter at 36/36 degenerate, so a real improvement is visible as a number and cannot be claimed without one; (c) every proof verified by 3.0.2 (**"both formats" was written before D-0046 and there is now only one**), since the chain is what `Order_reason` already emits and the row must not double-count it; (d) the coupling `Order_reason.weaken_declared` / `Encoding.expand_int_lin_le` must still agree on the constant — a standing hazard recorded in `WORKLOG.md` **D-0047 records the prediction this row falsifies**, written before this row reported so that it is a prediction and not a postdiction. Two outcomes and both are informative — if non-degenerate rows appear the cause was the reason; if they do not, the prediction is **wrong** and the weakness is in the elimination or reduction step, which promotes M2-L7 from detour to next thing to try. Whoever closes this row marks D-0047 with which happened. |
| M2-L13 | **The PB explanation for SOLVING**: propagate a learned PB row as a PB constraint over order literals, and take `to_linear_row` out of the propagation path | TODO — **the row D-0054 creates, and it supersedes "register the convertible rows"** | **D-0054 is the precondition: read it first.** A proof line and a propagation explanation are different objects. `Propagator.pb_row` is explicitly the *proof* one — `r_cid` required so a `pol` can cite it, frozen at **declared** bounds because "a row that moved with the search would be the wrong side of I-X6" — and PB analysis resolves over those, so what it learns is a proof object. `Learned.to_linear_row` then gates propagation on an **algebraic identity over the declared box**, a proof-side test doing a solving-side job. **The fix**: a counter/slack-based **PB propagator over `Lit.t`**, instantiated from `Learned.t` directly. The order encoding (D-0028) means those literals already exist; the clause propagator M2-L12 delivered is its **degree-1 case**, which is the type relationship D-0044 asserted all along; and the proof obligation is unchanged and already discharged, the row being on the page as an `ia` line with its `pol` behind it. **Inspiration is RoundingSat** — `reduce.ml`'s header already states the criterion ("reduce the reason so the resolvent stays **conflicting**"), which is solving-side, and confines it to the analysis; Elffers & Nordström (IJCAI 2018) and Koops et al. (CP 2025) are the reading, the latter now for **how RoundingSat keeps a learned constraint propagating**, not only for emission. **Tests**: (a) a scene where the learned PB row prunes and the clause over the same literals does not — this is the whole row, and M2-L6 looked for it and did not find it; (b) node count over the 39 models must **move**, against M2-L12's pinned 231; (c) every proof still verified by 3.0.2, since the propagation path changes and the emission path must not; (d) the declared consistency level is honest. **Do not measure success by `pb-convert`** — that counter is the proof-side gate this row removes |
| M2-L12 | **Make learned constraints propagate**: unit clauses as level-0 bounds first, then widen `bool_clause` to a threshold | **DONE 2026-09-18** (agent-prop) — **and the result is a measured NEGATIVE, reframed by D-0054.** Step 0 re-derived the clause-width histogram from an instrumented counter instead of proof text: **72 unit, 5 of width 2, 8 of width 3, 3 of width 4**, total 88 — confirming D-0052's provisional figure, so the plan held. All 88 learned clauses now have a runtime consumer. **And nothing prunes**: with propagation on versus off over all 39 models the search visits **231 nodes either way**, records **0 prunings** from a learned constraint, and emits **byte-identical proofs** (95332 bytes) — reproduced by the orchestrator. Cause traced: under the default branching (first-fail, smallest value first) the search re-branches the *same* variable down a spine, so a one-literal learned constraint is exactly the branch the search was about to take and is already in force when applied. Under a reversed order 2 of 39 models prune, and even there node count is unchanged — now a pinned equality, so a future change that *does* shrink the tree reddens. `bool_clause.ml` → `clause.ml`, widened to a threshold, declaring **`Bounds`**; `Clause.Bool_clause` keeps `Domain`. No watched literals. **`Retention` re-decided, not inherited**: the cap is now **soft** (a cited constraint is refused eviction, refusal counted) and `keep_all` stands on a **new** argument, D-0051's reversal condition being spent. 2190 ok / 0 FAIL, 39/39. **Read D-0054 before building on this**: it made a PROOF-shaped object propagate, which is why it achieved nothing | **75 of 88 learned clauses are proof-only today**, and `learn.ml`'s own header states the consequence: without a propagator the backjump "would re-reach the same fixpoint, re-take the same decision and re-derive the same conflict" — M2-L3's stated regret. **D-0052 settles the family question: this is a WIDENING of `bool_clause`, not a new family, so D-0044's bet is not spent** — `Explanation.Clause` is already `Lit.t list`, and `bool_clause`'s five Boolean-specific sites are constant substitutions. **Do it in two steps and do NOT do watched literals** (they would be the first mutable propagator state outside `Store`'s trail, and max observed clause width is 4). **(1)** unit learned clauses as level-0 bound tightenings — no propagator needed at all, justification is the existing `Clause` `rup`; **(2)** widen `bool_clause` to a threshold for the multi-literal remainder, declaring **`Bounds`** not `Domain` (order literals have an interior — `x≤1 ∨ x≥3` is a hole, not a tautology; ≥ 9 learned clauses of width ≥ 3 carry such a pair), keeping `Bool_clause` as the `Domain`-declaring `[0,1]` sub-module on the `Ne`/`Ne.Int_ne` pattern. **Must revisit `Retention` in the SAME change**: a trace line from a learned instance is RUP only while that constraint is live, which couples retention to the propagator set and is exactly the condition D-0051 named as reversing its keep-all verdict. **Before defending step (1)'s priority, re-derive the clause-width histogram from an instrumented run** — the "~70 of 88 are unit" figure is ±5 from proof-text parsing and D-0052 marks it provisional |

**Out of scope, recorded so it is not re-opened.** The arbitrary-constraint route — direct
inference between general constraints, Veksler & Strichman's `Infer(c1, c2) -> c*` — is
rejected in D-0044 on cost: the rules are per *ordered pair* of constraint types, and their
own third requirement is that the learned object be something the solver can already
propagate, which for us means a propagator plus an I-X10 classification per learned shape.
M4-T2 (Régin) is the row most likely to reach for it; it should not.

## M3 — reification, and the explanation question

| ID | Task | Status | Notes |
|---|---|---|---|
| M3-T0 | **Resolve D-0003** — what "higher-order explanation" means here | **DONE** | Resolved by **D-0026** (2026-09-16): (a) and (b) are layered, not alternatives — reasons are declarative data, justifications stay the reified cutting-planes expression. The evidence came from the GCS comparison, which found GCS answered (b) *against* the shape our `Deferred` assumed (~20% of runtime spent on reasons that "look lazy but are not"). This row sat TODO after the decision was taken; **M3-T2 has been unblocked since 2026-09-16** and nobody noticed |
| M3-T1 | `red`-based definitions for reified variables | **DONE 2026-09-18** (agent-reify). A reifier is an ordinary order-encoded bool (D-0007); its definition is **two rows**, FWD under witness `b -> 0` and BWD under `b -> 1`, each big-M the smallest that works. `reif_rows` is pure and shared by both doors so they cannot disagree. **Design recorded as D-0053**, whose load-bearing rule is that **`red` cannot give a MODEL-DECLARED reified bool its meaning** — `red` preserves satisfiability of the database, so a `b -> 0` witness cannot discharge a row that genuinely constrains `b`; measured, the checker rejects on *"Proofgoal 3"*, naming that very row. Hence a freshness precondition and the `.opb` door for model-stated reification. **722 insertions, 0 deletions; `Lit`/`Opb`/`Writer` untouched**, which was the wave's hard constraint. Verified by the orchestrator: 2154 ok / 0 FAIL (2129 + 25), 39/39 models. **Read D-0053 §2 before emitting any `red` anywhere**: over a contradictory database every `red` is accepted, wrong witness or none, and a missing witness is only a `Warning` | **M3-T2 inherits**: it can name any linear `<=` condition with one literal and cite `reif_fwd`/`reif_bwd` in a `pol`, and all five M3-T4 dispatcher cases over a `<=` condition are cutting-planes steps over these two rows. It **cannot** do `int_eq_reif`/`int_ne_reif`, which are not one pair — they need `p <-> (Σ<=c)`, `q <-> (Σ>=c)` and `b <-> p ∧ q`, and **the conjunction channelling is deliberately not built**. Nothing is wired into search yet: `ensure_direct` has no production caller, and a reifier registry needs an `Encoding.t` and a `Writer.t` together at pruning time (`justify.ml:146` is the only place in core holding a writer) |
| M3-T2 | Reified linear/comparison propagators + justifications | TODO | ~~blocked by T0~~ — **unblocked**, D-0026 settled it. Design-ready; needs `lib/proof/encoding.ml` (agent-proofhyg holds it this round) and should land with or after M3-T1. Build it through M3-T4's dispatcher rather than writing five cases twice **M3-T1 is DONE and this row inherits a named gap**: `int_eq_reif` / `int_ne_reif` are **not one pair**. `b <-> (Σ = c)` needs `p <-> (Σ<=c)`, `q <-> (Σ>=c)` and `b <-> p ∧ q` — `Encoding.reif_rows` gives `p` and `q`, but the **conjunction channelling is not built** and where `p`/`q` live (`.opb`, or `.pbp` behind `red`) is an open choice. Also: wiring the lazy door into search is this row's, not M3-T1's — see D-0053. |
| M3-T3 | Benchmark: proof-logging overhead vs. unlogged, report in `bench/` | DONE | first performance checkpoint — **superseded in scope by M3-T5** — absorbed by M3-T5, as its own note anticipated |
| M3-T4 | Reification dispatcher: author supplies enforce-hold / enforce-not-hold / entailment; framework does the collapse and contrapositive | TODO | Turns M3-T2 from "reified linear *and* reified comparison, each with five cases" into two propagators plus one shared helper (~200 lines). Known limitation to inherit knowingly: one trigger set per dispatcher. Depends on M2-T8 and M3-T0 |
| M3-T5 | Proof benchmark reporting `.opb` bytes, `.pbp` bytes **and** verify seconds separately | DONE | Absorbs M3-T3. GCS measured size and verify time moving in *opposite* directions at an identical search tree (5.9× smaller proof, 3.5× longer to check), so a size-only benchmark misleads. **Also the named falsifier for D-0026's performance prediction.** Take minimums, one at a time — `bench/run_bench.sh` + `bench/width_curve.sh`, `make bench`, not in the gate. Four separate columns and **no total**. **Headline is a null result**: 15 of 18 models sit at the measured process floor (4.0 ms solver, 4.6 ms checker) with 9-66% spread, so their timing columns measure `exec`, not this solver — no speed claim about either is supportable from the pre-existing suite. Byte columns are exact at every row. Format 2.0 vs 3.0: sizes agree with D-0023, every timing delta came back inside the spread and was labelled `noise`. **D-0026 falsifier NOT run** and said so — the layered form is M2-T8 and does not exist yet |

## M4 — global constraints

| ID | Task | Status | Notes |
|---|---|---|---|
| M4-T1 | `all_different` (bounds consistent) + Hall-interval justification | TODO | see D-0004. **Unblocked 2026-09-16 by D-0027**, which permits the multi-row Hall derivation outright. Land the ambient-row removal (M1-T31) first. Build T1 and T2 as *one propagator with two stages*, not two propagators (below) **This row is an experiment, not propagator number eight** (`docs/EXPLANATION-REVIEW.md` §3, and budget it as one). Everything green today is linear rows and clauses — the case nobody doubted. M4-T1 is the **first derivation that actually needs `Combine` / `Weaken` / `Model_row`**: a Hall justification cites many model rows in one tree (per Hall value a recovered at-most-one line, per Hall variable an at-least-one line — `GCS-COMPARISON.md` §3). The design anticipates exactly this, which is why `Model_row` exists; **anticipation is not evidence**, and this is the row that turns one into the other. It is therefore also where the project's central claim is first falsifiable. Land M2-T17 before it, so explanation size is already a tracked number when the first non-forced derivation lands. **D-0048 should be closed before this row, not after.** It asks whether a reified explanation is a research output — and this is the row where the reified form either demonstrates what it buys or looks like expensive ceremony. Decided before, the row is read as evidence; decided after, it is read as a verdict already reached. |
| M4-T2 | `all_different` domain consistent (Régin) — proof story is research-grade | TODO | **Design change from the GCS comparison**: one propagator, one consistency tag, two stages — the cheap value-consistency pass first, returning without an idempotence claim so cheaper propagators react before the matching/SCC work. GCS's cutoff is measured, not chosen: 256 var-value pairs, sitting at the geometric mean between a case 1.36× faster staged and one ~2% slower |
| M4-T3 | `array_int_element` | TODO | |
| M4-T4 | `int_times`, `int_div`, `int_abs` | TODO | **split into T4a/T4b below** |
| M4-T0 | Views (`±x + k`) and constants-as-variables in `Var`/`Store` | TODO | Should precede M4-T3: a 1-based `array_int_element` index is a view, not an auxiliary variable. GCS's aux-variable alternative "silently downgraded" every value-pruning propagator behind it (4/15 → 9/15 optimal on one family once fixed). Opens a proof-side question: each view needs its own range literals |
| M4-T4a | Integer interval arithmetic as a standalone, proof-free, unit-tested module | **DONE** | `lib/core/interval.ml` + `test/unit/test_interval.ml`, 94 brute-force checks. Exact functions assert **equality** with the enumerated hull; `quotient_filter`, documented sound-but-not-exact, asserts containment and the suite *counts and prints* the gap (40 of 4095). Deviates from GCS deliberately: GCS's `(n+1)/2` initial estimate in `isqrt` overflows at `max_int` and returns a **negative** root -- orchestrator reproduced the break, 3 checks go red. Verified 2026-09-16 |
| M4-T4b | `int_times`/`int_div`/`int_abs` over M4-T4a | TODO — **SPEC gate cleared 2026-09-16** | ~~Needs two SPEC additions first~~ — both are now written: **D-0033** and SPEC §2.1 "Integer division, modulo and absolute value" fix division rounding (GCS pins truncation toward zero, remainder taking the dividend's sign) and division by zero as *relational* rather than an error (zero has no support in the divisor's domain — matches MiniZinc and XCSP3). Anchor the family on one total `is_in_relation` predicate shared with `Model.check_assignment`. Depends on M4-T4a, M2-T8. **Inherited from M4-T4a, decide explicitly:** (1) `quotient_filter` is inexact in ~1% of small cases (40 of 4095, measured), inherited from JaCoP via GCS -- tightening it is a different and larger algorithm, so M4-T4b must decide whether it wants one; (2) the SPEC addition pins `int_div` to **truncation toward zero** while `Interval.div_floor`/`div_ceil` round **outward** -- these are the *relation* and the *bounds* respectively, they live two functions apart in one file, and using the relation's rounding to compute a bound prunes supported values. A header note in `interval.ml` warns about this. **Remaining blocker is a file claim, not a design gap**: it needs `lib/flatzinc/model.ml`, held by agent-oracle this round. Dispatch as soon as that claim is released. D-0033 also sets its hardest test: watch the *wrong* rounding produce a **rejected proof**, not merely the right one produce an accepted proof |
| M4-T5 | RUP hints on trace lines and nogoods, gated on M3-T5 showing they are needed | TODO | Hinted `rup` is O(hints), hint-free is O(live database) — worth 6–14× on a real model but 25% on a toy one. **All-or-nothing**: an engaged-but-empty hint list restricts propagation to nothing, so half-hinting the D-0018 trace chain breaks a working proof. Depends on M3-T5, M2-T9 **GATE EVALUATED 2026-09-18 (agent-hints), and the answer is NO — leave it closed.** Both dependencies (M3-T5, M2-T9) were already DONE, so the gate had been evaluable for some time and nobody had evaluated it. **The decisive fact: `width_root_unsat`, the largest proof in the suite, contains ZERO `rup` lines** (verified — its cost is one 1999-premise `pol` chain), so the biggest artefact here is **not evidence about RUP hints at all**. 36 of 37 non-width models sit in an 8.3–14.3 ms band with ±50–200% noise, i.e. at the process floor and carrying no signal, consistent with §3c's own finding that 23/38 models spend ≥90% of wall time outside the solver. The one informative model, `width_sat_depth` (862 lines, **342 `rup` lines**), grows ~2.5× absolute / ~4–5× floor-subtracted against a 9.2× growth in proof lines — **short of this row's own "6–14× on a real model" claim** and nowhere near the blow-up an O(live-database) cost predicts. **Migration cost is not the obstacle**: all `rup` emission funnels through `writer.ml:987`/`:994` with exactly **4 call sites** (`justify.ml:325,351,389`, `learned.ml:276`), so the all-or-nothing hazard is about correctness at those 4, not surface area. **Revisit when a real non-synthetic model enters the suite with a large live `rup` database and verify time as a non-floor share** — that is the missing evidence, and more measurement of the current suite will not supply it. |
| M4-T6 | **Spike** (not a build): in-proof tabulation as the GAC route for small-domain relations | TODO | Would give GAC arithmetic for ~10 lines per constraint with *no OPB change*, because the table is derived in-proof. **Unverified that this transfers to our order encoding** — that is why it is scoped as a spike. Depends on M4-T4b |

## M5 — optimisation

| ID | Task | Status | Notes |
|---|---|---|---|
| M5-T1 | Branch and bound, `obju` / `o` / `core` rules | TODO | |
| M5-T2 | `conclusion BOUNDS` for proved optimality | TODO | |
| M5-T3 | MiniZinc challenge instances as a regression set | TODO | |

## M6 — performance

Deliberately last. Do not start before M4 is green.

| ID | Task | Status | Notes |
|---|---|---|---|
| M6-T1 | Profile; establish the propagation hot path | TODO | |
| M6-T2 | Minor-GC pressure: move domains/trail to `Bigarray`/`Bytes` | TODO | the known OCaml risk, D-0001 |
| M6-T3 | Proof-writing buffered/batched off the hot path | TODO | |
| M6-T4 | Compare against Chuffed and the Glasgow solver on a shared set | TODO | |
