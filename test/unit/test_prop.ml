(* Unit tests for propagators (lib/core/prop/).

   Stub created by the orchestrator so that test/unit/dune can name this module before
   the propagator lands. M1-T7 replaces it. Follow test_core.ml's shape: a [check]
   counter, one function per area, exit 1 on any failure. *)

let () = print_endline "\npropagator unit tests: none yet (M1-T7)"
