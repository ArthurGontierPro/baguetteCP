(* Unit tests for the propagation engine and search (lib/core/engine.ml, search.ml).

   Orchestrator stub so test/unit/dune can name the module before M1-T10 lands. *)

let () = print_endline "\nengine unit tests: none yet (M1-T10)"
