(* M1-T15 / D-0018: corrupt one emitted proof step and assert veripb REJECTS it.
   Stub, owned by agent-mutate this round -- replace it wholesale, do not extend it.
   Named in test/unit/dune so the module exists before the work lands. *)

let () = print_endline "test_mutation: stub, M1-T15 not implemented yet (D-0018)"
