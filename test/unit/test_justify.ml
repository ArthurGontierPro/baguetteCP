(* Unit tests for Justify: Explanation.t rendered into VeriPB rules.

   Stub created by the orchestrator so that test/unit/dune can name this module before
   the bridge lands. M1-T7 replaces it. Follow test_proof.ml's shape. *)

let () = print_endline "\njustify unit tests: none yet (M1-T7)"
