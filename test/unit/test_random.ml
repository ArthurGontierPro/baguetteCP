(* M1-T16: stub, owned by agent-tests -- replace wholesale.
   Named in test/unit/dune so the module exists before the work lands. *)

let () = print_endline "test_random: stub, M1-T16 not implemented yet"
