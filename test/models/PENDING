# Models that are not expected to pass yet, one basename per line with a reason.
#
# The runner treats these as expected failures: they do not turn the gate red, but they
# are reported on every run so they cannot be forgotten. If a model listed here starts
# PASSING, the runner fails and tells you to delete the line — that is what stops this
# list from rotting into a list of quietly broken things.
#
# Delete a line the moment its model works. The file should be empty by M1-T11.

ne_sat          int_ne is rejected at compile.ml, not dropped. The propagator and its
#               proof exist and verify (M1-T9); what is missing is the CLI wiring.
#               D-0019: it does NOT need the direct encoding -- that was the old reason
#               on this line and it was wrong.
